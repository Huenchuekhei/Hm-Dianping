# 黑马点评 · 项目包装与 STAR 项目简历

> 一份可直接用于简历/面试的项目包装手册。所有描述均基于本仓库真实代码提炼，未夸大、可深挖。
> 文中标注「（补真实数据）」处，请替换为你自己的 JMeter 压测结果，面试时更有说服力。

---

## 一、项目包装

### 1. 一句话定位

> 仿「大众点评」的本地生活服务后端，以 **Redis 为核心**，系统解决**集群登录状态共享、缓存一致性、秒杀高并发**三大难题的高并发实战项目。

### 2. 项目描述（可直接粘贴到简历「项目经历」栏）

基于 Spring Boot + Redis + RabbitMQ + MySQL 构建的前后端分离本地生活服务平台，功能对标大众点评，涵盖**短信登录、商户查询缓存、优惠券秒杀、附近商户、UV 统计、用户签到、好友关注、达人探店**八大模块并形成业务闭环。

核心工作聚焦高并发场景：用 Redis 替代 Session 解决集群登录状态共享；基于 Cache Aside 模式 + 逻辑过期 + 互斥锁治理缓存穿透/击穿/雪崩；用 Redis 分布式锁（自研 → Redisson）保证「一人一单」；用 Redis + Lua 脚本原子化完成秒杀资格预检，配合乐观锁与 RabbitMQ 异步削峰，实现**零超卖**的秒杀下单。

### 3. 技术栈

| 分类 | 技术 |
|------|------|
| 后端框架 | Spring Boot 2.3、Spring MVC、MyBatis-Plus |
| 缓存 / 存储 | Redis（Lettuce 连接池）、MySQL 8 |
| 分布式能力 | Redisson（分布式锁）、RabbitMQ（异步削峰 + 死信队列） |
| 语言 / 工具 | Java 8、Lombok、Hutool、JMeter（压测） |

### 4. 功能模块与 Redis 数据结构映射（一图看全）

| 模块 | 核心技术点 | Redis 数据结构/能力 |
|------|-----------|-------------------|
| 短信登录 | 集群 Session 共享 + 双层拦截器 | Hash + String（token） |
| 商户缓存 | 缓存穿透/击穿/雪崩治理 | String + 逻辑过期 + 互斥锁 |
| 优惠券秒杀 | Lua 原子预检 + 乐观锁 + 异步削峰 | String（库存）+ Set（去重）+ Lua |
| 一人一单 | 分布式锁 | Redisson（可重入 + 看门狗） |
| 附近商户 | 按距离排序分页 | GEO（底层 ZSet） |
| 用户签到 | 签到 + 连续天数统计 | BitMap（BitField） |
| 好友关注 | 关注 / 共同关注 | Set（intersect 交集） |
| 达人探店 | 点赞排行 + Feed 流推送 | ZSet（score=时间戳） |
| UV 统计 | 独立访客估算 | HyperLogLog（≤16KB） |

### 5. 秒杀核心链路（一句话版）

`Lua 脚本原子预检（查库存 → 查重复 → 扣库存 → 记录）` → `发 RabbitMQ 异步下单` → `消费者用 Redisson 锁 + 乐观锁落库`。

---

## 二、STAR 法则项目简历（可直接粘贴）

### 【精简版 · 一屏简历适用】

**项目：黑马点评（本地生活服务平台后端）** · Java 8 / Spring Boot / Redis / RabbitMQ / MySQL

- **S（背景）**：本地生活平台在集群与高并发下，面临 Session 不共享、缓存击穿、秒杀超卖、数据库压力过大等痛点。
- **T（任务）**：搭建仿大众点评的后端平台，重点解决登录状态共享、缓存一致性、热点数据高可用与秒杀并发安全。
- **A（行动）**：
  - 用 Redis 存 token + 双层拦截器解决集群登录共享与自动续期；
  - 封装 CacheClient，以「空值缓存 + 逻辑过期 + 互斥锁 + 线程池异步重建」治理缓存穿透/击穿/雪崩；
  - 自研 setnx 分布式锁演进到 Redisson（可重入 + 看门狗），保证集群「一人一单」；
  - 用 Redis + Lua 原子化完成秒杀预检，配合乐观锁（`stock > 0`）与 RabbitMQ 异步削峰，实现零超卖；
  - 用 ZSet/Set/GEO/BitMap/HyperLogLog 分别实现点赞排行、共同关注、附近商户、签到与 UV 统计。
- **R（结果）**：缓存命中率大幅提升，热点数据高并发下稳定返回旧数据；秒杀零超卖；下单由串行改异步，吞吐量显著提升（JMeter 压测：异常率/平均耗时下降，**（补真实数据）**）。

### 【完整版 · 分四段】

**Situation（背景）**
本地生活类平台（对标大众点评）在推广期会面临瞬时高并发：秒杀活动导致海量请求打向 MySQL，传统单体 + Session 架构在集群部署下出现登录状态丢失、热点数据缓存失效后 DB 被打穿、并发扣库存超卖、下单接口串行阻塞等典型问题。

**Task（任务）**
独立完成后端高并发改造：①实现集群环境下的登录状态共享与自动续期；②建立可靠的缓存体系，保证热点数据高可用；③设计一套「预检 → 异步下单 → 落库」的秒杀链路，做到不超卖、低延迟、高吞吐；④用合适的 Redis 数据结构支撑点赞、关注、附近商户、签到等高频功能。

**Action（行动）**
1. **登录**：验证码与用户信息（Hash）存入 Redis，token 作为 key，双层拦截器（第一层刷新 token + 写 ThreadLocal，第二层校验登录返回 401），解决多 Tomcat 下 Session 不共享问题。
2. **缓存**：封装 CacheClient 工具类，落地三种策略——「空值缓存」防穿透、「逻辑过期 + 互斥锁 + 10 线程池异步重建」防击穿、更新时先写库再删缓存（Cache Aside）保证一致性。
3. **分布式锁**：先自研 SimpleRedisLock（setnx + 线程标识 + Lua 原子释放防误删），后演进到 Redisson 的 RLock（可重入 + WatchDog 看门狗续期），用于「一人一单」。
4. **秒杀**：预检逻辑下沉到 `seckill.lua`，在 Redis 内一次性完成「查库存 → 判重复 → 扣库存 → 记订单 → 发消息」，保证原子性；Java 端执行脚本后把订单投递 RabbitMQ，消费者用 Redisson 锁 + 乐观锁（`where stock > 0`）落库，杜绝超卖。
5. **全局 ID**：自研 RedisIdWorker，用「相对时间戳左移 32 位 | Redis 自增序列」生成订单号，支撑分布式唯一 ID。
6. **高频功能**：ZSet（score=时间戳）做点赞排行榜，Set 交集做共同关注，GEO 做附近商户按距离分页，BitMap 做签到与连续天数，HyperLogLog 做 UV 估算。

**Result（结果）**
- 热点数据高并发下稳定返回旧数据并异步重建，DB 压力显著下降；
- 秒杀链路从「串行查库 + 锁」改造为「Lua 预检 + MQ 异步」，吞吐量提升、异常率与平均耗时下降（**（补真实数据）**）；
- 实现零超卖、一人一单；多模块复用 Redis 数据结构，内存占用可控（如 UV 仅 ≤16KB）。

---

## 三、亮点难点精讲（面试问答）

> 每条按「做了什么 → 为什么 → 面试怎么说」展开，覆盖面试官高频追问。

### 1. 缓存穿透 / 击穿 / 雪崩（最高频，必考）

| 问题 | 定义 | 本项目方案 |
|------|------|-----------|
| 穿透 | 请求**不存在的 key**，缓存没数据，直接打库 | 空值缓存（缓存 `""`，TTL 2 分钟） |
| 击穿 | **单个热点 key 失效**，大量并发同时打库 | 逻辑过期 + 互斥锁 + 线程池异步重建 |
| 雪崩 | **大量 key 同时失效**，瞬时压垮 DB | 逻辑过期避免同时失效 + 随机 TTL + 缓存预热 |

- **穿透追问**：除了空值缓存，还有布隆过滤器（先判断 key 是否存在，不存在直接返回）；空值方案要设短 TTL，否则会长期占用内存且掩盖数据新增。
- **击穿追问**：为什么用「逻辑过期」而非「互斥锁」？——互斥锁是「时间换空间」，一致性高但把并行变串行、易死锁；逻辑过期是「空间换时间」，主线程直接返回旧数据、后台异步重建，响应快但会短暂脏读。本项目商户详情读多写少、容忍短暂不一致，故选逻辑过期。

### 2. 缓存一致性与 Cache Aside

- 更新策略：**先更新数据库，再删除缓存**（`update` 方法里 `updateById` 后 `redisTemplate.delete`）。
- 为什么不先删缓存再更库？——先删缓存会出现「删了缓存、还没更库，另一个请求把旧值又读回缓存」的窗口，造成脏数据；先更库再删缓存窗口更小。
- 进阶：若要强一致，可用 Redisson 读写锁（读共享、写排他），保证「读写互斥」。

### 3. 分布式锁演进（自研 → Redisson）

| 阶段 | 实现 | 解决的问题 |
|------|------|-----------|
| v1 setnx | `setIfAbsent` 加锁 | 基本互斥 |
| v2 + 超时 | setnx 带 TTL | 防宕机死锁 |
| v3 + 线程标识 | value 存线程 ID | 防误删别人锁 |
| v4 Lua 释放 | `unlock.lua` 先比对再删 | 保证「判断 + 删除」原子 |
| v5 Redisson | 可重入 + 看门狗 + 可重试 | 续期、重入、高可用 |

- **为什么最终用 Redisson**：自研锁无法解决「业务还没执行完锁就过期」的续期问题；Redisson 的 WatchDog 每隔一段时间检查持有者是否还持有锁，持有则自动续期，且用 Hash 结构记录重入次数，支持可重入。
- **集群下单机锁失效的原因**：`synchronized`/本地锁只能锁住当前 JVM，多台服务器部署时各锁各的，无法互斥，必须用「多进程共享」的 Redis 锁。

### 4. 秒杀：Lua 原子预检 + 乐观锁 + 异步削峰

- **为什么用 Lua**：Redis 单条命令是原子的，但「查库存 → 判重复 → 扣库存 → 记录」是多条命令，中间会被插入。把多条命令放进 Lua 脚本，Redis 把整个脚本当原子操作执行，天然互斥、还减少网络往返。
- **Lua 返回值协议**：`0` 成功、`1` 库存不足、`2` 重复下单、`-1` 库存 key 缺失。
- **超卖怎么解**：乐观锁，SQL 用 `update ... set stock = stock - 1 where voucher_id = ? and stock > 0`，`stock > 0` 即 CAS 思想的落地——只要还有库存才扣，从根上杜绝扣成负数。
- **异步削峰**：预检通过后不直接落库，而是投递 RabbitMQ，立即返回订单号给前端；消费者后台慢慢写库，把「耗时长的 DB 写」从请求主链路剥离。

### 5. 消息队列：为什么从 Stream 演进到 RabbitMQ

- 早期用 Redis Stream 的消费者组（XREADGROUP + XACK）做异步下单，具备消息回溯、分组消费、pending-list 重试等能力。
- 但 Stream 的**消息确认、死信、重试语义需要手写**，可靠性不如成熟 MQ；最终改用 **RabbitMQ + 死信队列（DLQ）**：普通队列设置 TTL（10s），超时/异常消息自动进入死信交换机 → 死信队列，由独立消费者兜底处理，保证消息不丢。

### 6. 全局唯一 ID（RedisIdWorker）

- 用「相对时间戳（相对 2022-01-01）左移 32 位 | Redis 当天自增序列」拼出 64 位 ID，单日单 key 可支撑约 40 亿量级，满足秒杀订单号需求。

### 7. 多数据结构选型（面试官爱问「为什么不用 MySQL」）

| 功能 | 选型 | 理由 |
|------|------|------|
| 点赞排行 | ZSet | score=时间戳，天然有序，取 top5 是 O(logN) |
| 共同关注 | Set 交集 | `intersect` 一条命令求交集 |
| 附近商户 | GEO | 底层 ZSet，范围查询 + 距离排序内建 |
| 签到 | BitMap | 一个 bit 记一天，内存极小，bitField 算连续天数 |
| UV | HyperLogLog | 概率估算基数，≤16KB 即可估上亿 UV（误差 <0.81%） |
| Feed 流 | ZSet 收件箱 | 粉丝收件箱模式，score 时间戳滚动分页 |

- **统一话术**：这些数据都是「高频读写、低价值、允许一定误差」，放 MySQL 会拖垮主业务；Redis 内存操作 + 内建结构，兼顾性能与内存。

### 8. 登录：双层拦截器

- **第一层（RefreshTokenInterceptor）**：拦截所有请求，从 header 取 token，查 Redis 拿用户、写 ThreadLocal、刷新 token 有效期 30 分钟。
- **第二层（LoginInterceptor）**：只拦需要登录的路径，ThreadLocal 无用户则返回 401。
- **为什么两层**：如果只有一层且不拦所有请求，部分请求不会刷新 token 导致掉线；如果只有一层且拦所有请求，无法区分「可匿名访问」与「必须登录」。两层实现「全局续期 + 按需鉴权」的职责分离。

---

## 四、代码中发现的真实可改进点（面试加分项，体现你读过代码）

1. **MQ 交换机命名不一致**：`VoucherOrderServiceImpl` 里发送用 `convertAndSend("X", "XA", ...)`，而 `QueueConfig` 声明的是交换机 `Common` + 队列 `CQ`，二者对不上，会导致消息发送失败或需补声明。面试时主动提出说明你对链路是清楚的，属**加分项**。
2. **Lua 脚本残留 Stream 逻辑**：`seckill.lua` 末尾仍 `xadd stream.orders`，但 Java 端已改走 RabbitMQ，`xadd` 属冗余（无害但可清理）。
3. **库存 key 需预热**：Lua 里库存 key `seckill:stock:{id}` 若为 nil 会返回 -1，说明秒杀前需先把库存写入 Redis（缓存预热）。
4. **`proxy` 字段未初始化**：`handleVoucherOrder` 中 `proxy.createVoucherOrder(...)` 依赖的代理对象在 RabbitMQ 方案下未被赋值，若走此分支需注意 Spring AOP 自调用失效问题。

---

*以上「完整版 STAR」可直接整段贴入简历项目经历；「精简版」适合一屏简历。讲解部分建议配合代码过一遍，面试时按「定义 → 方案 → 取舍」三句话回答。*
