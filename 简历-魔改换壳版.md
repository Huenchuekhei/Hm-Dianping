# 项目简历 · 换壳版（基于魔改升级后的真实能力）

> 适用前提：`黑马点评-魔改升级落地清单.md` 的 **P1 五项已完成**（幂等/补偿/对账、支付闭环、压测留数、多级缓存、限流）。
> 红线：文中所有【 】为压测/实测数字占位，**必须用你自己的真实数据回填**，没有数据就删掉该句，不要编造。
> 红线：不要写成"某公司实习项目"。项目名和业务场景可以重定位，但**不能虚构雇主**——面试官问一句团队分工就穿帮，且属于简历造假。

---

## 一、项目包装方案

### 1. 项目名称（三选一，简历只写一个）

| 方案 | 名称 | 定位语 |
|---|---|---|
| **主推** | **CityGo · 本地生活服务平台** | 参考大众点评核心业务模型**自研**的本地生活平台，主打高并发优惠券营销与探店社交 |
| 备选 A | 探店 Town · 校园生活圈平台 | 面向校园场景的商铺推荐 + 优惠券秒杀 + 探店社区一体化平台 |
| 备选 B | 惠淘 · 本地优惠券营销平台 | 弱化"点评"影子，聚焦"优惠券秒杀 + 订单交易"电商交易属性 |

> 选择逻辑：面试官对"点评类"已有审美疲劳，把叙事重心从"商铺展示"挪到"**高并发交易 + 可靠性**"，技术含金量立刻不同。下面全文以 CityGo 为例。

### 2. 角色与时间写法

```
项目名称：CityGo · 本地生活服务平台（个人全栈设计开发）
项目时间：20XX.XX – 20XX.XX
技术栈：Spring Boot / Redis(Redisson·Lua·PubSub) / RabbitMQ / Caffeine / MySQL / MyBatis-Plus / JMeter
项目链接：github.com/你的仓库（强烈建议放开源仓库，可信度 +30%）
```

- 角色：**独立设计并实现**（校招认可度高于"参与者"，前提是每条都能讲深）。
- 不要写"项目简介：仿大众点评"——写成"参考主流本地生活平台业务模型，自主设计并落地"。

### 3. 被问"这是不是黑马点评"时的标准口径（背下来）

> "最初是跟着一个 Redis 实战课程入门的，但课程版本只有单机内存队列和最基础的缓存 Demo。我在它的业务骨架上做了重构：把异步链路整体迁移到 RabbitMQ 并补齐了幂等、重试、补偿、对账这套可靠性闭环，补上了课程完全缺失的支付与超时关单，加了多级缓存和限流，所有改造都用 JMeter 压测留了前后对比数据。可以把改造前后的设计差异展开讲。"

——主动承认起点 + 立刻甩出课程里没有的深度，比藏着掖着安全得多；面试官接下来会顺着你的改造点问，正好进入你准备好的主场。

---

## 二、项目经历 · 精简版（一页简历直接粘贴）

**CityGo · 本地生活服务平台**（个人独立开发）　　Spring Boot / Redis / RabbitMQ / Caffeine / MySQL

**项目描述**：参考大众点评业务模型自主设计的本地生活平台，覆盖商铺检索、优惠券秒杀、订单支付、探店社交（Feed 流 / 点赞 / 关注 / 签到）完整链路。核心工作围绕**高并发秒杀的可靠性闭环**与**热点查询性能**展开，全部改造经 JMeter 压测量化验证。

- 设计"**Redis+Lua 资格预检 → RabbitMQ 异步下单**"秒杀链路：Lua 原子完成库存判断、一人一单判重与 Redis 预扣，DB 以 `stock>0` 乐观锁最终裁决；1000 并发压测下 **QPS 由【X】提升至【Y】，零超卖、零重复订单**；
- 针对 MQ 异步链路补齐**消息可靠性闭环**：`user_id+voucher_id` 唯一索引实现幂等消费，TTL+死信队列实现 **10s 有限重试（上限 3 次防毒丸）**，重试耗尽自动回补 Redis 库存并落补偿表；另设**5 分钟定时对账**核对 Redis/DB 双侧库存与订单数，不一致落差异表告警；
- 补齐平台缺失的**支付与订单闭环**：设计"未支付→已支付/已取消"订单状态机，基于 TTL+死信实现 **15 分钟延迟关单**，支付与关单均以数据库 **CAS** 更新互斥，杜绝并发重复支付与"先关单后支付"竞态；关单同步回补 Redis/DB 双侧库存并释放一人一单资格，延迟消息丢失场景由对账任务兜底扫描；
- 构建 **Caffeine+Redis+MySQL 三级缓存**：L1（W-TinyLFU，容量/短 TTL 主动过期）消除 Redis 网络 RTT，Redis Pub/Sub 广播失效保证多节点一致性，启动时按热度榜 TopN 预热；热点接口 **P99 RT 由【X】ms 降至【Y】ms**；
- 通过 JMeter 压测定位秒杀接口**性能塌陷区（QPS≈【X】）**，据此设计 `@RateLimit` 注解 + Redis ZSet **滑动窗口限流**（Lua 原子执行），叠加"全局容量"与"单用户频控"双维度，超限请求 429 快速失败，阈值内流量无感；
- 基于 Redis 多数据结构实现社交模块：**ZSet 点赞排行榜、Set 共同关注、GEO 附近商铺（距离排序分页）、BitMap 连续签到统计、HyperLogLog 日 UV 统计**，Feed 流采用推模式收件箱 + 游标滚动分页，解决推流分页重复问题。

---

## 三、项目经历 · 详版（网申大框 / 二页简历适用）

**项目背景（S/T）**
本地生活平台的核心营销手段是限量优惠券秒杀，瞬时流量可达日常百倍；同时商铺详情等热点读多写少，缓存一致性直接影响体验。项目目标：在高并发下保证**不超卖、不少卖、可对账、可支付**的交易闭环，并将热点查询控制在毫秒级。

**核心工作（A）——按面试展开顺序排列**

1. **登录与网关层**
   - Redis Hash 存储登录态 + 双层拦截器实现 30 分钟滑动续期，解决集群 Session 不共享；
   - （P2 完成后可写）升级 JWT+Redis 双 Token：accessToken 2h 无状态 + refreshToken 7d 可吊销，`/user/refresh` 无感续期，黑名单实现主动踢人。

2. **秒杀与消息可靠性（最大亮点，留足展开时间）**
   - 链路：Lua 原子预检（库存 + 一人一单 + 预扣）→ RabbitMQ 异步落库（DB 乐观锁裁决）→ 唯一索引幂等 → TTL 死信重试 → 补偿回补 → 定时对账；
   - 关键取舍：Redis 与 DB 是两套账，**不追求强一致**，用"幂等 + 重试 + 补偿 + 对账"四层保障最终一致；分布式锁在 Lua 前置判重后退化为一道可选优化，正确性交由唯一索引兜底；
   - 量化：1000 并发压测 QPS【X→Y】，RT P99【X ms】，错误率 0%，对账差异 0。

3. **订单与支付闭环**
   - 状态机：1 未支付 → 2 已支付 / 4 已取消（超时）；
   - 延迟关单：RabbitMQ TTL+死信 15min，CAS（`WHERE status=1`）防与支付互斥冲突；关单双回补（DB+Redis）并 `SREM` 判重集合允许重新抢购；
   - 兜底：延迟消息可能丢失 → 对账任务扫描超时未关单订单补偿关单；
   - 面试可延伸：延迟任务方案对比（轮询 / DelayQueue / Redis 过期监听 / RabbitMQ TTL / RocketMQ 延迟消息 / 时间轮）及各自局限。

4. **多级缓存与预热**
   - L1 Caffeine（W-TinyLFU，maxSize 1000，写后 60s 主动过期——以秒级不一致换零网络 RTT）→ L2 Redis 逻辑过期（互斥重建，返回旧值）→ DB 空值缓存防穿透；
   - 多节点一致性：更新侧删 L2 + Pub/Sub 广播，各节点 invalidate L1；
   - 预热：启动 Runner 按浏览热度榜 TopN 装载 L1/L2；热度与 UV 数据由查询埋点（ZINCRBY / PFADD）自然沉淀，**组件之间形成体系**。

5. **限流与容量保护**
   - 先压测定位塌陷区（RT 陡增点），阈值 = 塌陷区 QPS×0.8；
   - 自研 `@RateLimit`（AOP + Redis ZSet 滑动窗口 Lua）：全局维度保护容量，USER 维度（5 次/秒）防单用户脚本刷单，两者叠加；
   - 429 快速失败 + 前端友好降级，压测验证阈值内流量 RT 无劣化。

6. **社交与 LBS 模块**（一笔带过，展示广度即可）
   - Feed 流推模式收件箱 + `lastId+offset` 游标分页（解决翻页取重）；点赞 ZSet 排行；共同关注 Set 交集；GEO 附近商铺距离排序分页；BitMap 连续签到；HLL 日 UV（12KB 估算百万级访客，误差 0.81%）。

**成果（R）**
- 秒杀链路 1000 并发压测：QPS 【X】→【Y】（+【Z】%），零超卖零重复订单，对账零差异；
- 热点商铺查询 P99 RT：【X】ms →【Y】ms；
- 压测报告、可靠性时序图、对账日志等物证均已入库仓库 README。

---

## 四、专业技能栏（与项目强绑定，可直接粘贴）

- 熟练使用 **Redis** 常用数据结构（String/Hash/Set/ZSet/BitMap/GEO/HyperLogLog）及其业务场景，掌握缓存穿透/击穿/雪崩治理（空值缓存、互斥锁、逻辑过期）、**Caffeine 多级缓存**与一致性方案（Cache Aside / 逻辑过期 / Pub/Sub 失效广播 / 定时对账）；
- 掌握 **RabbitMQ** 异步削峰、生产者 confirm 确认、TTL+死信队列实现**延迟关单与有限重试**，理解消息幂等（唯一索引）、重复消费、消息丢失与补偿的对治方案；
- 熟悉高并发秒杀设计：**Lua 脚本原子预检、乐观锁防超卖、库存预热、滑动窗口限流**，理解方案边界与最终一致性取舍；
- 熟悉 **MySQL** 事务与索引原理，能进行慢 SQL 定位与优化；熟悉 **Spring Boot** 自动装配、AOP、事务传播机制；
- 熟练使用 **JMeter** 进行接口压测与容量评估，能给出限流阈值与性能塌陷区定位的量化依据。

> 写法要点：技能栏每一条都能被项目经历某一条佐证，面试官"按图索骥"时条条有落点；不要出现项目里没有的词（如没做 ES/微服务就别写"了解 Elasticsearch/Spring Cloud"，写了就是邀请对方考你）。

---

## 五、换壳对照表（课程点 → 新叙事，防止口径漂移）

| 原课程说法（禁用） | 换壳后说法 |
|---|---|
| "黑马点评项目" | "CityGo · 本地生活服务平台（自研）" |
| "学习了 Redis 实战课程" | 简历不写；面试被问时按第一节口径主动交代 |
| "使用 Redis Stream 实现消息队列" | "基于 RabbitMQ TTL+死信实现异步下单、延迟关单与有限重试" |
| "使用 setnx / Redisson 实现一人一单" | "Lua 预检 + DB 唯一索引幂等兜底一人一单，Redisson 作为并发削峰期的可选保护" |
| "解决了缓存穿透/击穿/雪崩" | "构建三级缓存体系（Caffeine/Redis/DB），含失效广播与热点预热，P99 RT 量化数据" |
| "实现了优惠券秒杀" | "设计秒杀可靠性闭环：预检→异步→幂等→重试→补偿→对账，零超卖" |
| "项目部署在本地" | 完成 P2-5 后写"Docker Compose 一键部署 + Prometheus 监控"；未完成则不主动提部署 |

---

## 六、面试叙述主线（30 秒电梯版 + 演进链）

**电梯版**：
> "这个平台我最核心的工作是把优惠券秒杀做成了完整的可靠性闭环：入口用 Lua 把资格判断压到 Redis 单线程里原子完成，DB 只做最终裁决；下单走 RabbitMQ 异步削峰，幂等、重试、补偿、对账四层保障最终一致；再往上用压测定位性能塌陷区，配了双维度滑动窗口限流；读侧用 Caffeine+Redis 三级缓存加预热，P99 从【X】ms 降到【Y】ms。"

**预埋追问链**（每层都有准备好的答案，主动引导面试官往这走）：

```
setnx 手写锁 → 不可重入/误删/续期三大缺陷 → Redisson(可重入+看门狗+hash结构)
→ Lua 前置判重后锁的职责变化 → 唯一索引兜底
→ 为什么不追求 Redis/DB 强一致 → 四层最终一致保障 → 对账差异怎么处理
→ TTL 延迟队列的队头阻塞局限 → RocketMQ 延迟消息/事务消息升级路径
→ 压测数字怎么来的 → 塌陷区怎么定位 → 限流阈值为何 ×0.8
→ 多级缓存集群一致性 → Caffeine 参数依据 → 预热 TopN 从哪来(热度榜)
```

---

## 七、完成度自查（投递前勾选）

- [ ] P1 五项全部完成并验收（对照落地清单附录 B）；
- [ ] 所有【X】【Y】已替换为真实压测数据，且能说出压测机配置与参数；
- [ ] README 已同步为 CityGo 口径（项目描述、架构图、压测对照表），GitHub 仓库链接可访问；
- [ ] 简历中每个技术名词都能在代码中 grep 到（防"文档与代码不符"穿帮）；
- [ ] 被问"是不是课程项目"的口径已背熟（第一节第 3 点）。
