# 黑马点评魔改升级 · 接口文档（含源码实现）

> 版本：v1.0 ｜ 生成日期：2026-08-29
> 依据：`黑马点评-魔改升级落地清单.md`（P1-1/P1-2/P1-4/P1-5 全量 + P2 选做）
> 用途：作为项目迭代的直接开发依据。每个接口按 **接口说明 / 请求方式 / 请求路径 / 请求参数格式 / 返回值格式 / 实现源码** 组织，MQ 通信补充 **交换机 / 路由键 / 队列** 契约。
> 代码基线：`D:\JavaProject\hm-dianping`（SpringBoot 2.3.12 / Java 8 / RabbitMQ 已引入 / Redisson 3.13.6 / MyBatis-Plus 3.4.3）

---

## 目录

- [全局约定](#全局约定)
- [第 0 章 全局基础设施改造](#第-0-章-全局基础设施改造)
- [第 1 章 秒杀与订单模块（P1-1 消息可靠性 + P1-2 支付闭环）](#第-1-章-秒杀与订单模块)
- [第 2 章 缓存模块（P1-4 多级缓存 + 预热）](#第-2-章-缓存模块)
- [第 3 章 限流组件（P1-5）](#第-3-章-限流组件)
- [第 4 章 P2 进阶接口（选做）](#第-4-章-p2-进阶接口选做)
- [附录 A 改造文件对照表](#附录-a-改造文件对照表)
- [附录 B 验收与压测清单](#附录-b-验收与压测清单)

---

## 全局约定

### 1. 统一响应结构（`com.hmdp.dto.Result`，已有，不改动）

```json
{
  "success": true,
  "errorMsg": null,
  "data": 1578558664933920770,
  "total": null
}
```

> `application.yaml` 已配置 `jackson.default-property-inclusion: non_null`，为 null 的字段实际不输出。本文档示例中的 `null` 字段仅为说明结构。

### 2. 登录态

- 请求头：`authorization: {token}`（前端已按此传递）。
- 拦截器现状：`/voucher/**`、`/shop/**`、`/user/login` 等在 `MvcConfig` 白名单中放行；**`/voucher-order/**` 需要登录**（秒杀、支付、订单查询均为登录接口）。
- 用户上下文：`UserHolder.getUser()` 返回 `UserDTO`。

### 3. 订单状态机（`tb_voucher_order.status`，字段已存在）

```
                    ┌────────────── 支付成功(CAS 1→2) ──────────────┐
                    │                                              ▼
  [下单] ──► 1 未支付 ──┤                                        2 已支付 ──► 3 已核销
                    │
                    └── 15分钟超时(CAS 1→4) ──► 4 已取消（库存回补，允许重新抢购）
```

| 状态值 | 含义 | 说明 |
|---|---|---|
| 1 | 未支付 | Lua 预检通过、订单异步落库后的初始状态 |
| 2 | 已支付 | 支付接口 CAS 更新 |
| 3 | 已核销 | 预留，本期不实现 |
| 4 | 已取消 | 超时关单，触发库存回补（Redis + DB 双回补） |

### 4. Redis Key 约定（`RedisConstants` 新增部分见 0.3）

| Key | 类型 | 用途 |
|---|---|---|
| `seckill:stock:{voucherId}` | String | 秒杀券 Redis 库存（已有） |
| `seckill:order:{voucherId}` | Set | 一人一单判重集合（已有） |
| `seckill:path:{voucherId}:{userId}` | String | P2-7 动态秒杀路径，TTL 60s |
| `uv:shop:{yyyy:MM:dd}` | HyperLogLog | 当日店铺浏览 UV |
| `hot:shop` | ZSet | 店铺浏览热度榜（score=浏览次数） |
| `cache:evict:shop` | Pub/Sub 频道 | 多节点本地缓存失效广播 |
| `rate_limit:*` | ZSet | 滑动窗口限流 |

### 5. MQ 拓扑总览（改造后，详见 0.5）

```
                         ┌────────────────────────────────────────────────┐
                         │              DirectExchange "X"                │
                         └────────────────────────────────────────────────┘
             XA │            XR │             XD │              XC │
                ▼               ▼                ▼                 ▼
        order.queue      order.retry.queue  order.delay.queue  order.close.queue
        （下单消费）      （TTL 10s 重试）    （TTL 15min 延迟）    （关单执行）
                │ 消费失败 nack        │ TTL 到期            │ TTL 到期
                └──► XR ──► retry ──┘──► XA 回到 order.queue   └──► XC ──► close
                         │ 重试≥3次（x-death）→ 补偿：Redis 回补库存 + 记录补偿表
```

---

# 第 0 章 全局基础设施改造

## 0.1 pom.xml 新增依赖

**接口说明**：新增多级缓存依赖；P2 相关依赖注释预留，按需开启。

```xml
<!-- P1-4：Caffeine 本地缓存 -->
<dependency>
    <groupId>com.github.ben-manes.caffeine</groupId>
    <artifactId>caffeine</artifactId>
    <version>2.9.3</version>
</dependency>

<!-- P2-1：RocketMQ（选做，启用时打开） -->
<!--
<dependency>
    <groupId>org.apache.rocketmq</groupId>
    <artifactId>rocketmq-spring-boot-starter</artifactId>
    <version>2.2.3</version>
</dependency>
-->

<!-- P2-3：Elasticsearch（选做，启用时打开） -->
<!--
<dependency>
    <groupId>org.elasticsearch.client</groupId>
    <artifactId>elasticsearch-rest-high-level-client</artifactId>
    <version>7.12.1</version>
</dependency>
<dependency>
    <groupId>org.elasticsearch</groupId>
    <artifactId>elasticsearch</artifactId>
    <version>7.12.1</version>
</dependency>
-->

<!-- P2-5：可观测（选做，启用时打开） -->
<!--
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>
<dependency>
    <groupId>io.micrometer</groupId>
    <artifactId>micrometer-registry-prometheus</artifactId>
</dependency>
-->
```

## 0.2 application.yaml 新增 RabbitMQ 可靠性配置

**接口说明**：开启生产者发送确认（publisher-confirm）与强制回退（mandatory），消费端改为手动 ack。

```yaml
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    # ===== 新增：消息可靠性 =====
    publisher-confirm-type: correlated   # 异步确认，消息到达交换机后回调
    publisher-returns: true              # 路由失败时回退消息
    template:
      mandatory: true
    listener:
      simple:
        acknowledge-mode: manual         # 消费端手动 ack
        prefetch: 1
        concurrency: 2
        max-concurrency: 8
```

> ⚠️ 迁移提示：`QueueConfig` 里的旧队列 `QA` 带 TTL 参数，新队列 `order.queue` 参数不同。**若 RabbitMQ 控制台中已存在旧 QA/QD，先在管理台删除旧队列**，再启动应用自动创建新队列。

## 0.3 RedisConstants 新增常量

**接口说明**：`src/main/java/com/hmdp/utils/RedisConstants.java` 末尾追加。

```java
    // ==================== 魔改升级新增 ====================
    /** 动态秒杀路径 */
    public static final String SECKILL_PATH_KEY = "seckill:path:";
    /** 店铺浏览 UV（按天） */
    public static final String SHOP_UV_KEY = "uv:shop:";
    /** 店铺浏览热度榜 */
    public static final String SHOP_HOT_KEY = "hot:shop";
    /** 本地缓存失效广播频道 */
    public static final String CACHE_EVICT_TOPIC = "cache:evict:shop";
    /** 滑动窗口限流 key 前缀 */
    public static final String RATE_LIMIT_KEY = "rate_limit:";
    /** 订单超时未支付时长（分钟），与延迟队列 TTL 保持一致 */
    public static final long ORDER_UNPAID_TTL_MINUTES = 15L;
```

## 0.4 SQL 变更脚本

**接口说明**：幂等唯一索引（一人一单的 DB 兜底）+ 消费补偿表 + 对账差异表。保存为 `src/main/resources/db/upgrade_v1.sql`。

```sql
-- 1. 一人一单 DB 兜底：唯一索引（幂等消费的最终防线）
--    注意：一个用户对一个秒杀券仅允许一单，业务上成立
ALTER TABLE `tb_voucher_order`
    ADD UNIQUE KEY `uk_user_voucher` (`user_id`, `voucher_id`);

-- 2. 消费补偿表：重试耗尽的死信订单，记录后回补库存
DROP TABLE IF EXISTS `tb_order_compensation`;
CREATE TABLE `tb_order_compensation` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `order_id` bigint NOT NULL COMMENT '订单id',
  `user_id` bigint NOT NULL COMMENT '用户id',
  `voucher_id` bigint NOT NULL COMMENT '秒杀券id',
  `reason` varchar(255) DEFAULT NULL COMMENT '进入补偿的原因',
  `status` tinyint NOT NULL DEFAULT 0 COMMENT '0：待处理；1：已处理（库存已回补）',
  `create_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_order_id` (`order_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='秒杀订单消费补偿表';

-- 3. 对账差异表：定时对账任务的不一致记录
DROP TABLE IF EXISTS `tb_stock_reconcile_log`;
CREATE TABLE `tb_stock_reconcile_log` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `voucher_id` bigint NOT NULL COMMENT '秒杀券id',
  `redis_stock` int DEFAULT NULL COMMENT 'Redis 库存',
  `db_stock` int DEFAULT NULL COMMENT 'DB 库存',
  `db_order_count` bigint DEFAULT NULL COMMENT 'DB 有效订单数(状态1/2)',
  `redis_order_count` bigint DEFAULT NULL COMMENT 'Redis 判重集合大小',
  `diff_desc` varchar(255) DEFAULT NULL COMMENT '差异描述',
  `create_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_voucher_id` (`voucher_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='库存对账差异日志表';
```

## 0.5 MQ 常量与队列拓扑改造

**接口说明**：用语义化常量替换散落在代码中的 `"X"/"XA"` 字面量；重建 4 条队列（下单、重试、延迟关单、关单执行）。

**新增** `src/main/java/com/hmdp/config/MqConstants.java`：

```java
package com.hmdp.config;

/**
 * RabbitMQ 拓扑常量：交换机 / 队列 / 路由键
 */
public class MqConstants {
    /** 业务交换机（同时充当死信交换机） */
    public static final String ORDER_EXCHANGE = "X";

    /** 路由键：秒杀下单 */
    public static final String KEY_ORDER_CREATE = "XA";
    /** 路由键：下单消费失败重试（经 TTL 延迟后重回 XA） */
    public static final String KEY_ORDER_RETRY = "XR";
    /** 路由键：延迟关单入口（TTL 到期后转 XC） */
    public static final String KEY_ORDER_DELAY = "XD";
    /** 路由键：关单执行 */
    public static final String KEY_ORDER_CLOSE = "XC";

    /** 队列：秒杀订单落库（消费队列） */
    public static final String QUEUE_ORDER_CREATE = "order.queue";
    /** 队列：重试延迟队列（TTL 10s，无消费者，到期重回 order.queue） */
    public static final String QUEUE_ORDER_RETRY = "order.retry.queue";
    /** 队列：延迟关单队列（TTL 15min，无消费者，到期转入 order.close.queue） */
    public static final String QUEUE_ORDER_DELAY = "order.delay.queue";
    /** 队列：关单执行队列 */
    public static final String QUEUE_ORDER_CLOSE = "order.close.queue";

    /** 重试队列 TTL：10s */
    public static final long TTL_RETRY_MS = 10_000L;
    /** 延迟关单 TTL：15min（与 ORDER_UNPAID_TTL_MINUTES 一致） */
    public static final long TTL_CLOSE_MS = 15 * 60 * 1000L;
    /** 最大重试次数（x-death 计数），超过则进入补偿 */
    public static final int MAX_RETRY = 3;
}
```

**整体替换** `src/main/java/com/hmdp/config/QueueConfig.java`：

```java
package com.hmdp.config;

import org.springframework.amqp.core.*;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.HashMap;
import java.util.Map;

import static com.hmdp.config.MqConstants.*;

/**
 * MQ 拓扑：
 *  X --XA--> order.queue          下单消费队列（消费失败 nack → 死信 XR）
 *  X --XR--> order.retry.queue    重试延迟队列（TTL 10s，到期死信 → XA 回到下单队列）
 *  X --XD--> order.delay.queue    延迟关单队列（TTL 15min，到期死信 → XC）
 *  X --XC--> order.close.queue    关单执行队列
 */
@Configuration
public class QueueConfig {

    @Bean("xExchange")
    public DirectExchange xExchange() {
        return new DirectExchange(ORDER_EXCHANGE);
    }

    /** 下单消费队列：失败死信到 XR（重试延迟队列） */
    @Bean("orderQueue")
    public Queue orderQueue() {
        return QueueBuilder.durable(QUEUE_ORDER_CREATE)
                .withArgument("x-dead-letter-exchange", ORDER_EXCHANGE)
                .withArgument("x-dead-letter-routing-key", KEY_ORDER_RETRY)
                .build();
    }

    /** 重试延迟队列：TTL 10s，到期死信回 XA，形成重试闭环 */
    @Bean("orderRetryQueue")
    public Queue orderRetryQueue() {
        return QueueBuilder.durable(QUEUE_ORDER_RETRY)
                .withArgument("x-message-ttl", (int) TTL_RETRY_MS)
                .withArgument("x-dead-letter-exchange", ORDER_EXCHANGE)
                .withArgument("x-dead-letter-routing-key", KEY_ORDER_CREATE)
                .build();
    }

    /** 延迟关单队列：TTL 15min，到期死信到 XC（关单执行队列） */
    @Bean("orderDelayQueue")
    public Queue orderDelayQueue() {
        return QueueBuilder.durable(QUEUE_ORDER_DELAY)
                .withArgument("x-message-ttl", (int) TTL_CLOSE_MS)
                .withArgument("x-dead-letter-exchange", ORDER_EXCHANGE)
                .withArgument("x-dead-letter-routing-key", KEY_ORDER_CLOSE)
                .build();
    }

    /** 关单执行队列 */
    @Bean("orderCloseQueue")
    public Queue orderCloseQueue() {
        return QueueBuilder.durable(QUEUE_ORDER_CLOSE).build();
    }

    @Bean
    public Binding orderQueueBinding(@Qualifier("orderQueue") Queue queue,
                                     @Qualifier("xExchange") DirectExchange exchange) {
        return BindingBuilder.bind(queue).to(exchange).with(KEY_ORDER_CREATE);
    }

    @Bean
    public Binding orderRetryQueueBinding(@Qualifier("orderRetryQueue") Queue queue,
                                          @Qualifier("xExchange") DirectExchange exchange) {
        return BindingBuilder.bind(queue).to(exchange).with(KEY_ORDER_RETRY);
    }

    @Bean
    public Binding orderDelayQueueBinding(@Qualifier("orderDelayQueue") Queue queue,
                                          @Qualifier("xExchange") DirectExchange exchange) {
        return BindingBuilder.bind(queue).to(exchange).with(KEY_ORDER_DELAY);
    }

    @Bean
    public Binding orderCloseQueueBinding(@Qualifier("orderCloseQueue") Queue queue,
                                          @Qualifier("xExchange") DirectExchange exchange) {
        return BindingBuilder.bind(queue).to(exchange).with(KEY_ORDER_CLOSE);
    }
}
```

**新增** `src/main/java/com/hmdp/config/MqConfirmConfig.java`（发送确认回调，只做记录与告警日志）：

```java
package com.hmdp.config;

import lombok.extern.slf4j.Slf4j;
import org.springframework.amqp.rabbit.connection.CorrelationData;
import org.springframework.amqp.rabbit.core.RabbitTemplate;
import org.springframework.stereotype.Component;

import javax.annotation.PostConstruct;
import javax.annotation.Resource;

/**
 * 生产者发送确认：消息是否到达交换机（confirm）与是否路由到队列（return）
 */
@Slf4j
@Component
public class MqConfirmConfig {

    @Resource
    private RabbitTemplate rabbitTemplate;

    @PostConstruct
    public void init() {
        // confirm：消息是否成功到达交换机
        rabbitTemplate.setConfirmCallback((CorrelationData correlationData, boolean ack, String cause) -> {
            if (!ack) {
                log.error("[MQ确认] 消息未到达交换机，correlationData={}，原因={}", correlationData, cause);
                // P2-1 落地前，此处由「对账任务」兜底发现（见 1.9）
            }
        });
        // return：交换机收到但未路由到任何队列
        rabbitTemplate.setReturnsCallback(returned ->
                log.error("[MQ回退] 消息路由失败，exchange={}，routingKey={}，replyText={}",
                        returned.getExchange(), returned.getRoutingKey(), returned.getReplyText()));
    }
}
```

## 0.6 秒杀 Lua 脚本改造

**接口说明**：删除已废弃的 `XADD stream.orders`（Stream 消费者已被 RabbitMQ 方案替代，属于死代码）；保留库存判断 + 一人一单判重 + 原子扣减。

**整体替换** `src/main/resources/seckill.lua`：

```lua
-- 1.参数列表
--1.1.优惠券id
local voucherId = ARGV[1]
--1.2.用户id
local userId = ARGV[2]

-- 2.数据key
--2.1.库存key
local stockKey = 'seckill:stock:' .. voucherId
--2.2.订单key（一人一单判重集合）
local orderKey = 'seckill:order:' .. voucherId

-- 3.脚本业务
--3.1.判断库存是否充足（防御 key 不存在，避免 tonumber(nil) 报错）
local stock = tonumber(redis.call('get', stockKey))
if stock == nil then
    return -1
end
if (stock <= 0) then
    --3.2.库存不足，返回 1
    return 1
end
--3.3.判断用户是否已下单
if (redis.call('sismember', orderKey, userId) == 1) then
    --3.4.重复下单，返回 2
    return 2
end
--3.5.扣库存
redis.call('incrby', stockKey, -1)
--3.6.记录下单用户（关单时 SREM 回补）
redis.call('sadd', orderKey, userId)
--3.7.资格预检通过，返回 0（订单落库由 RabbitMQ 异步完成）
return 0
```

---

# 第 1 章 秒杀与订单模块

## 1.1 优惠券秒杀下单（改造）

| 项目 | 内容 |
|---|---|
| 接口说明 | 用户提交秒杀请求。Lua 脚本原子完成"库存判断 + 一人一单判重 + Redis 预扣库存"，通过后生成订单号并发送 MQ 异步落库，**同步链路不触碰数据库**。相比原版：① 删除 Stream 死代码；② 发送失败时回补 Redis 库存并告知用户；③ 引入限流（见 3.3）。 |
| 请求方式 | `POST` |
| 请求路径 | `/voucher-order/seckill/{id}` |
| 请求头 | `authorization: {token}` |

**请求参数格式**：

```json
// 路径参数
{
  "id": "10"   // 秒杀券 voucherId
}
```

**返回值格式**：

```json
// 成功（data 为订单号，前端凭此跳转待支付页）
{"success": true, "data": 1578558664933920770}

// 失败
{"success": false, "errorMsg": "库存不足"}
{"success": false, "errorMsg": "不能重复下单"}
{"success": false, "errorMsg": "系统繁忙，请稍后再试"}
```

**实现源码** —— 整体替换 `VoucherOrderServiceImpl` 的 `seckillVoucher` 方法（该类完整重构版见 1.2 末尾）：

```java
    @Override
    public Result seckillVoucher(Long voucherId) {
        // 1.秒杀资格预检（Lua 原子执行：判库存 + 判一人一单 + Redis 预扣）
        Long userId = UserHolder.getUser().getId();
        long orderId = redisIdWorker.nextId("order");
        Long result = stringRedisTemplate.execute(
                SECKILL_SCRIPT,
                Collections.emptyList(),
                voucherId.toString(), userId.toString(), String.valueOf(orderId)
        );
        int r = result == null ? -1 : result.intValue();
        if (r != 0) {
            if (r == -1) {
                return Result.fail("秒杀活动不存在或尚未开始");
            }
            return Result.fail(r == 1 ? "库存不足" : "不能重复下单");
        }

        // 2.封装订单消息，异步落库
        VoucherOrder order = new VoucherOrder();
        order.setId(orderId);
        order.setUserId(userId);
        order.setVoucherId(voucherId);
        try {
            rabbitTemplate.convertAndSend(
                    MqConstants.ORDER_EXCHANGE, MqConstants.KEY_ORDER_CREATE,
                    JSONUtil.toJsonStr(order));
        } catch (Exception e) {
            // 3.发送失败：回补 Redis 预扣库存 + 判重集合，保证不超卖不少卖
            log.error("发送下单消息失败，订单ID:{}，开始回补库存", orderId, e);
            rollbackRedisStock(voucherId, userId);
            return Result.fail("系统繁忙，请稍后再试");
        }
        // 4.返回订单号，前端轮询订单状态（见 1.7）
        return Result.ok(orderId);
    }

    /** Redis 库存回补：消费耗尽补偿与发送失败共用 */
    public void rollbackRedisStock(Long voucherId, Long userId) {
        stringRedisTemplate.opsForValue().increment(RedisConstants.SECKILL_STOCK_KEY + voucherId);
        stringRedisTemplate.opsForSet().remove("seckill:order:" + voucherId, userId.toString());
    }
```

> 面试要点：Lua 预检在 Redis 侧"预扣"，DB 侧在消费者中二次扣减（`stock > 0` 乐观锁）。Redis 与 DB 是两套账，**一致性由"幂等消费 + 重试 + 补偿 + 定时对账"四层保障**（1.2 / 1.4 / 1.9），只承诺最终一致。

## 1.2 秒杀订单异步落库（MQ 消费）

| 项目 | 内容 |
|---|---|
| 接口说明 | 消费下单队列，在事务中完成"DB 扣库存（乐观锁）→ 订单落库（唯一索引幂等）→ 发送 15 分钟延迟关单消息"。修复原版两处缺陷：**绕过 DB 层一人一单校验**、无幂等保障。 |
| 请求方式 | MQ 异步消费（RabbitMQ） |
| 队列契约 | exchange：`MqConstants.ORDER_EXCHANGE`（"X"）；routingKey：`MqConstants.KEY_ORDER_CREATE`（"XA"）；queue：`order.queue`；ack 模式：手动 |
| 请求路径 | -- |

**请求参数格式**（消息体）：

```json
{
  "id": "1578558664933920770",   // 订单id
  "userId": "1010",              // 用户id
  "voucherId": "10"              // 秒杀券id
}
```

**返回值格式**：--（消费成功 `basicAck`；重复消息 ack 吞掉；失败 nack 进重试队列，重试 ≥3 次走补偿，见 1.4）

**实现源码** —— 新增 `src/main/java/com/hmdp/listener/OrderCreateListener.java`（替代原 `SeckillVoucherListener.receivedA`）：

```java
package com.hmdp.listener;

import cn.hutool.json.JSONUtil;
import com.hmdp.config.MqConstants;
import com.hmdp.entity.VoucherOrder;
import com.hmdp.service.impl.VoucherOrderServiceImpl;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.amqp.core.Message;
import org.springframework.amqp.rabbit.annotation.RabbitListener;
import org.springframework.amqp.rabbit.core.RabbitTemplate;
import org.springframework.dao.DuplicateKeyException;
import org.springframework.stereotype.Component;

import com.rabbitmq.client.Channel;
import javax.annotation.Resource;
import java.io.IOException;
import java.util.List;
import java.util.Map;

import static com.hmdp.config.MqConstants.MAX_RETRY;

/**
 * 秒杀订单消费者：幂等落库 + 延迟关单消息投递 + 重试/补偿
 */
@Slf4j
@Component
@RequiredArgsConstructor
public class OrderCreateListener {

    @Resource
    private VoucherOrderServiceImpl voucherOrderService;
    @Resource
    private RabbitTemplate rabbitTemplate;

    @RabbitListener(queues = MqConstants.QUEUE_ORDER_CREATE)
    public void onOrderCreate(Message message, Channel channel) throws IOException {
        long tag = message.getMessageProperties().getDeliveryTag();
        VoucherOrder order = JSONUtil.toBean(new String(message.getBody()), VoucherOrder.class);
        int retryCount = getXDeathCount(message);

        try {
            // 1.重试耗尽：不再 nack（会无限循环），直接走补偿并 ack
            if (retryCount >= MAX_RETRY) {
                log.error("[下单消费] 重试{}次仍失败，进入补偿。order={}", retryCount, order);
                voucherOrderService.compensate(order, "重试" + retryCount + "次失败");
                channel.basicAck(tag, false);
                return;
            }
            // 2.事务内：DB 扣库存 → 唯一索引幂等落库 → 发延迟关单消息
            voucherOrderService.createVoucherOrder(order);
            channel.basicAck(tag, false);
            log.info("[下单消费] 订单落库成功，orderId={}", order.getId());
        } catch (DuplicateKeyException e) {
            // 3.唯一索引冲突 = 重复消费/重复下单 → 幂等吞掉
            log.warn("[下单消费] 重复消息，幂等跳过。orderId={}", order.getId());
            channel.basicAck(tag, false);
        } catch (Exception e) {
            // 4.其他异常：nack 不重回原队列 → 死信到重试延迟队列（TTL 后自动重回）
            log.error("[下单消费] 消费失败，第{}次重试。orderId={}", retryCount + 1, order.getId(), e);
            channel.basicNack(tag, false, false);
        }
    }

    /** 从 x-death 头读取已重试次数 */
    @SuppressWarnings("unchecked")
    private int getXDeathCount(Message message) {
        Object xDeath = message.getMessageProperties().getHeaders().get("x-death");
        if (xDeath instanceof List && !((List<?>) xDeath).isEmpty()) {
            Map<String, Object> death = (Map<String, Object>) ((List<?>) xDeath).get(0);
            Object count = death.get("count");
            return count == null ? 0 : Integer.parseInt(count.toString());
        }
        return 0;
    }
}
```

**改造** `VoucherOrderServiceImpl`：删除原 `handleVoucherOrder`（Redisson 锁 + 代理调用链整体废弃，一人一单改由**唯一索引兜底**）、删除注释掉的 Stream/阻塞队列历史代码。核心事务方法与补偿方法如下（完整类结构见下）：

```java
    /**
     * 订单落库（由 MQ 消费者调用，外部调用事务才会生效）
     * 事务内三步：DB 乐观锁扣库存 → 唯一索引幂等落库 → 投递延迟关单消息
     */
    @Transactional
    public void createVoucherOrder(VoucherOrder order) {
        // 1.DB 扣库存：where stock > 0 乐观锁防超卖（DB 侧最终裁决）
        boolean success = seckillVoucherService.update()
                .setSql("stock = stock - 1")
                .eq("voucher_id", order.getVoucherId())
                .gt("stock", 0)
                .update();
        if (!success) {
            // 抛异常 → 事务回滚 → nack 重试 → 3 次后补偿回补 Redis（见 1.4）
            throw new IllegalStateException("库存不足，voucherId=" + order.getVoucherId());
        }
        // 2.落库：uk_user_voucher 唯一索引兜底一人一单，冲突抛 DuplicateKeyException 回滚
        order.setStatus(1);                       // 1 未支付
        order.setPayType(1);                      // 1 余额支付（模拟）
        order.setCreateTime(LocalDateTime.now());
        save(order);
        // 3.投递延迟关单消息（15 分钟后消费；发送失败则整个事务回滚，等待重试）
        rabbitTemplate.convertAndSend(
                MqConstants.ORDER_EXCHANGE, MqConstants.KEY_ORDER_DELAY,
                JSONUtil.toJsonStr(order.getId()));
    }

    /**
     * 消费补偿：重试耗尽/发送失败后的兜底
     * 记录补偿表 + 回补 Redis 库存与判重集合（DB 库存无需回补：事务已回滚，DB 从未扣减）
     */
    public void compensate(VoucherOrder order, String reason) {
        OrderCompensation compensation = new OrderCompensation();
        compensation.setOrderId(order.getId());
        compensation.setUserId(order.getUserId());
        compensation.setVoucherId(order.getVoucherId());
        compensation.setReason(reason);
        compensation.setStatus(1);
        compensation.setCreateTime(LocalDateTime.now());
        compensationMapper.insert(compensation);
        rollbackRedisStock(order.getVoucherId(), order.getUserId());
        log.error("[补偿] 已回补 Redis 库存并记录补偿表。order={}, reason={}", order, reason);
    }
```

`VoucherOrderServiceImpl` 需要新增的注入与 import：

```java
    @Resource
    private OrderCompensationMapper compensationMapper;

    // 新增 import
    import com.hmdp.config.MqConstants;
    import com.hmdp.entity.OrderCompensation;
    import com.hmdp.mapper.OrderCompensationMapper;
    import java.time.LocalDateTime;
```

> 面试要点：为什么删掉了 Redisson 锁？——资格判定已前移到 Lua（Redis 单线程原子），落库冲突由**唯一索引**兜底，锁退化为"减少唯一键冲突报错"的可选优化，不再承担正确性职责。这是"锁的职责演进"的标准答法。

## 1.3 消费重试（MQ 延迟重投）

| 项目 | 内容 |
|---|---|
| 接口说明 | 下单消费失败后 `basicNack(requeue=false)`，消息死信路由到重试延迟队列，TTL 10s 到期后自动回到下单队列重新消费，形成**指数意义上的有限重试闭环**（最多 `MAX_RETRY=3` 次）。 |
| 请求方式 | MQ 延迟重投（无消费者，纯 TTL + DLX） |
| 队列契约 | exchange：`X`；routingKey：`XR`；queue：`order.retry.queue`（`x-message-ttl=10000`，死信回 `XA`） |
| 请求路径 | -- |

**请求参数格式 / 返回值格式**：--（消息体与 1.2 相同）

**实现源码**：无消费者。队列声明见 0.5 `QueueConfig.orderRetryQueue()`。流程：`order.queue 消费抛异常 → nack → XR → order.retry.queue → (10s) → XA → order.queue`，重试次数由 1.2 的 `x-death` 计数控制。

> 面试要点：为什么不用 `requeue=true`？——立即重回队头会造成 CPU 空转打满（毒丸消息）；TTL 延迟重投给了下游恢复时间。生产级方案可换成 RabbitMQ 延迟插件（`x-delayed-message`）或直接上 RocketMQ。

## 1.4 消费耗尽补偿（死信兜底 + 库存回补）

| 项目 | 内容 |
|---|---|
| 接口说明 | 重试 ≥3 次仍失败（通常是真实库存耗尽而 Redis 未同步等场景），不再重试：**记录补偿表 + 回补 Redis 预扣库存与判重集合**，让其他用户可以继续抢，差异由对账任务最终核对。 |
| 请求方式 | MQ 消费器内联补偿（见 1.2 源码 `retryCount >= MAX_RETRY` 分支） |
| 队列契约 | 复用 `order.queue`（x-death 头计数），无独立死信队列 |
| 请求路径 | -- |

**实现源码**：见 1.2 `OrderCreateListener` + `VoucherOrderServiceImpl.compensate()` + 补偿表实体。

**新增** `src/main/java/com/hmdp/entity/OrderCompensation.java`：

```java
package com.hmdp.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.experimental.Accessors;

import java.io.Serializable;
import java.time.LocalDateTime;

@Data
@EqualsAndHashCode(callSuper = false)
@Accessors(chain = true)
@TableName("tb_order_compensation")
public class OrderCompensation implements Serializable {
    private static final long serialVersionUID = 1L;

    @TableId(value = "id", type = IdType.AUTO)
    private Long id;
    private Long orderId;
    private Long userId;
    private Long voucherId;
    private String reason;
    /** 0：待处理；1：已处理（库存已回补） */
    private Integer status;
    private LocalDateTime createTime;
}
```

**新增** `src/main/java/com/hmdp/mapper/OrderCompensationMapper.java`：

```java
package com.hmdp.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.hmdp.entity.OrderCompensation;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface OrderCompensationMapper extends BaseMapper<OrderCompensation> {
}
```

## 1.5 订单超时关单（MQ 延迟消息）

| 项目 | 内容 |
|---|---|
| 接口说明 | 订单落库成功即投递"15 分钟延迟消息"（TTL+死信实现）；到期后消费关单消息，用 **CAS（`status=1 → 4`）** 关闭超时未支付订单，并回补 Redis 与 DB 双侧库存、清除一人一单判重，允许用户重新抢购。 |
| 请求方式 | MQ 异步消费（RabbitMQ，TTL+DLX 延迟队列） |
| 队列契约 | 入口 exchange/routingKey：`X` / `XD`；延迟队列 `order.delay.queue`（`x-message-ttl=900000`，死信 → `XC`）；消费队列 `order.close.queue`；ack：手动 |
| 请求路径 | -- |

**请求参数格式**（入口消息体，由 1.2 事务内投递）：

```json
{
  "id": "1578558664933920770"   // 仅需订单id
}
```

**返回值格式**：--（关单成功 / 订单已支付 → 都 ack；关单 CAS 失败且状态为未支付以外时直接 ack 吞掉）

**实现源码** —— 新增 `src/main/java/com/hmdp/listener/OrderCloseListener.java`（替代原 `SeckillVoucherListener.receivedD`，**原来的 QD 消费者逻辑有重复落库 Bug，直接废弃**）：

```java
package com.hmdp.listener;

import cn.hutool.json.JSONUtil;
import com.hmdp.config.MqConstants;
import com.hmdp.entity.SeckillVoucher;
import com.hmdp.entity.VoucherOrder;
import com.hmdp.service.ISeckillVoucherService;
import com.hmdp.service.impl.VoucherOrderServiceImpl;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.amqp.core.Message;
import org.springframework.amqp.rabbit.annotation.RabbitListener;
import org.springframework.data.redis.core.StringRedisTemplate;
import org.springframework.stereotype.Component;

import com.rabbitmq.client.Channel;
import javax.annotation.Resource;
import java.io.IOException;

import static com.hmdp.utils.RedisConstants.SECKILL_STOCK_KEY;

/**
 * 订单超时关单消费者：
 * 1. CAS 关闭超时未支付订单（status 1 → 4）
 * 2. 关单成功后回补 DB 库存 + Redis 库存，SREM 一人一单判重集合
 */
@Slf4j
@Component
@RequiredArgsConstructor
public class OrderCloseListener {

    @Resource
    private VoucherOrderServiceImpl voucherOrderService;
    @Resource
    private ISeckillVoucherService seckillVoucherService;
    @Resource
    private StringRedisTemplate stringRedisTemplate;

    @RabbitListener(queues = MqConstants.QUEUE_ORDER_CLOSE)
    public void onOrderClose(Message message, Channel channel) throws IOException {
        long tag = message.getMessageProperties().getDeliveryTag();
        Long orderId = JSONUtil.toBean(new String(message.getBody()), Long.class);
        try {
            VoucherOrder order = voucherOrderService.getById(orderId);
            // 订单不存在（异常数据）或已支付/已取消：无需关单，直接 ack
            if (order == null || order.getStatus() == null || order.getStatus() != 1) {
                log.info("[关单] 订单状态非未支付，跳过关单。orderId={}，status={}",
                        orderId, order == null ? null : order.getStatus());
                channel.basicAck(tag, false);
                return;
            }
            // CAS 关单：仅当仍为"未支付"时置为"已取消"（与支付接口的 CAS 互斥，二者只能成功一个）
            boolean closed = voucherOrderService.update()
                    .set("status", 4)
                    .eq("id", orderId)
                    .eq("status", 1)
                    .update();
            if (!closed) {
                // 刚好完成支付，放弃关单
                log.info("[关单] CAS 失败（已支付），放弃关单。orderId={}", orderId);
                channel.basicAck(tag, false);
                return;
            }
            // 回补 DB 库存（关单属于库存释放，不做 stock 上限判断）
            seckillVoucherService.update()
                    .setSql("stock = stock + 1")
                    .eq("voucher_id", order.getVoucherId())
                    .update();
            // 回补 Redis 预扣库存 + 释放一人一单资格（允许重新抢购）
            stringRedisTemplate.opsForValue().increment(SECKILL_STOCK_KEY + order.getVoucherId());
            stringRedisTemplate.opsForSet().remove("seckill:order:" + order.getVoucherId(),
                    order.getUserId().toString());
            log.info("[关单] 订单超时关闭完成，库存已回补。orderId={}", orderId);
            channel.basicAck(tag, false);
        } catch (Exception e) {
            log.error("[关单] 关单异常，orderId={}", orderId, e);
            channel.basicNack(tag, false, false);
        }
    }
}
```

> 面试要点（延迟任务方案对比，按演进顺序讲）：定时轮询扫表（延迟高、全表扫描）→ JDK DelayQueue（单机、宕机丢）→ Redis 过期监听（漏触发、不保证）→ **RabbitMQ TTL+死信（本方案，零新增组件）** → RocketMQ 延迟消息（P2-1 升级路径）→ Redisson DelayedQueue。
> 已知局限（主动讲出来是加分项）：TTL 队列是队头判定，若队头消息 TTL 比后面长会造成阻塞——本方案所有延迟消息 TTL 相同（15min），不存在该问题；若未来支持"可变时长关单"，需换延迟插件或时间轮。延迟消息本身可能丢失，因此 1.9 对账任务含"超时未关单兜底扫描"。

## 1.6 模拟支付

| 项目 | 内容 |
|---|---|
| 接口说明 | 用户对待支付订单发起支付（本地模拟第三方支付成功回调；支付宝沙箱对接为 P2 扩展）。以 **CAS（`status=1 → 2`）** 更新，天然防并发重复支付、防"先关单后支付"的竞态。 |
| 请求方式 | `POST` |
| 请求路径 | `/voucher-order/pay/{id}` |
| 请求头 | `authorization: {token}` |

**请求参数格式**：

```json
// 路径参数
{
  "id": "1578558664933920770"   // 订单id
}
```

**返回值格式**：

```json
{"success": true, "data": {"orderId": "1578558664933920770", "payTime": "2026-08-29 15:30:00"}}
{"success": false, "errorMsg": "订单不存在或无权操作"}
{"success": false, "errorMsg": "订单已超时关闭，支付失败"}
{"success": false, "errorMsg": "请勿重复支付"}
```

**实现源码** —— `VoucherOrderController` 新增：

```java
    /**
     * 模拟支付（第三方支付成功回调）
     */
    @PostMapping("pay/{id}")
    public Result pay(@PathVariable("id") Long orderId) {
        return voucherOrderService.payOrder(orderId);
    }
```

`IVoucherOrderService` 新增接口方法 `Result payOrder(Long orderId);`，实现：

```java
    @Override
    public Result payOrder(Long orderId) {
        // 1.归属校验
        Long userId = UserHolder.getUser().getId();
        VoucherOrder order = getById(orderId);
        if (order == null || !order.getUserId().equals(userId)) {
            return Result.fail("订单不存在或无权操作");
        }
        // 2.支付成功回调：CAS status 1→2（与关单消费者互斥，双方只能成功一个）
        LocalDateTime now = LocalDateTime.now();
        boolean paid = update()
                .set("status", 2)
                .set("pay_time", now)
                .set("pay_type", 1)
                .eq("id", orderId)
                .eq("status", 1)
                .update();
        if (paid) {
            Map<String, Object> data = new HashMap<>();
            data.put("orderId", orderId);
            data.put("payTime", now.format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss")));
            return Result.ok(data);
        }
        // 3.CAS 失败：区分"已支付"与"已超时关闭"
        Integer status = getById(orderId).getStatus();
        return Result.fail(status != null && status == 2 ? "请勿重复支付" : "订单已超时关闭，支付失败");
    }
```

> P2 扩展占位：真实支付宝沙箱接入时，本接口改为预下单（返回支付二维码/跳转表单），新增 `POST /voucher-order/pay/notify` 接收异步回调，回调内执行与上面相同的 CAS 逻辑 + 验签。回调未到达时由对账任务驱动 `alipay.trade.query` 主动查单。

## 1.7 查询订单状态

| 项目 | 内容 |
|---|---|
| 接口说明 | 秒杀返回订单号后，前端轮询本接口直到订单从"处理中"变为"未支付/已取消"，或支付后查询支付结果。仅订单本人可查。 |
| 请求方式 | `GET` |
| 请求路径 | `/voucher-order/{id}` |
| 请求头 | `authorization: {token}` |

**请求参数格式**：

```json
{ "id": "1578558664933920770" }
```

**返回值格式**：

```json
{
  "success": true,
  "data": {
    "id": "1578558664933920770",
    "userId": "1010",
    "voucherId": "10",
    "payType": 1,
    "status": 1,
    "createTime": "2026-08-29 15:20:00",
    "payTime": null,
    "useTime": null,
    "refundTime": null,
    "updateTime": "2026-08-29 15:20:00"
  }
}
```

**实现源码** —— `VoucherOrderController` 新增：

```java
    /**
     * 查询订单状态（仅本人）
     */
    @GetMapping("{id}")
    public Result queryOrder(@PathVariable("id") Long orderId) {
        Long userId = UserHolder.getUser().getId();
        VoucherOrder order = voucherOrderService.getById(orderId);
        if (order == null || !order.getUserId().equals(userId)) {
            return Result.fail("订单不存在或无权操作");
        }
        return Result.ok(order);
    }
```

## 1.8 我的订单列表

| 项目 | 内容 |
|---|---|
| 接口说明 | 分页查询当前用户的订单列表，按下单时间倒序。 |
| 请求方式 | `GET` |
| 请求路径 | `/voucher-order/list` |
| 请求头 | `authorization: {token}` |

**请求参数格式**：

```json
// Query 参数
{ "current": "1" }   // 页码，默认 1，每页 10 条
```

**返回值格式**：

```json
{
  "success": true,
  "data": [
    { "id": "1578558664933920770", "voucherId": "10", "status": 1, "createTime": "2026-08-29 15:20:00" }
  ],
  "total": 23
}
```

**实现源码** —— `VoucherOrderController` 新增：

```java
    /**
     * 我的订单列表（分页）
     */
    @GetMapping("list")
    public Result listOrders(@RequestParam(value = "current", defaultValue = "1") Integer current) {
        Long userId = UserHolder.getUser().getId();
        Page<VoucherOrder> page = voucherOrderService.query()
                .eq("user_id", userId)
                .orderByDesc("create_time")
                .page(new Page<>(current, SystemConstants.DEFAULT_PAGE_SIZE));
        return Result.ok(page.getRecords(), page.getTotal());
    }
```

> 需在文件头部补充 import：`com.baomidou.mybatisplus.extension.plugins.pagination.Page`、`com.hmdp.utils.SystemConstants`、`org.springframework.web.bind.annotation.*`（改为 `.*` 以覆盖 `GetMapping/RequestParam`）。

## 1.9 库存对账定时任务（含超时未关单兜底扫描）

| 项目 | 内容 |
|---|---|
| 接口说明 | 定时对账兜底最终一致性：① 比对 Redis 库存 vs DB 库存、DB 有效订单数 vs Redis 判重集合大小；② 扫描"创建超过 15+5 分钟仍为未支付"的订单执行关单（兜底延迟消息丢失）。差异写 `tb_stock_reconcile_log` 并打 WARN。同时提供手动触发接口便于演示与压测后核对。 |
| 请求方式 | 定时任务（每 5 分钟）+ HTTP 手动触发 |
| 请求路径 | `POST /voucher-order/reconcile`（手动触发） |
| 请求头 | `authorization: {token}`（演示用，生产应加管理员权限校验） |

**返回值格式**：

```json
{
  "success": true,
  "data": {
    "checkedVouchers": 8,
    "inconsistent": 0,
    "rescuedTimeoutOrders": 2,
    "diffs": []
  }
}
```

**实现源码** —— `VoucherOrderServiceImpl` 新增：

```java
    /**
     * 对账：Redis 库存 vs DB 库存 / DB 有效订单数 vs Redis 判重集合
     * 返回不一致差异列表；diff 为空代表账目一致
     */
    public Map<String, Object> reconcile() {
        List<SeckillVoucher> vouchers = seckillVoucherService.list();
        List<Map<String, Object>> diffs = new ArrayList<>();
        for (SeckillVoucher sv : vouchers) {
            Long vid = sv.getVoucherId();
            String redisStockStr = stringRedisTemplate.opsForValue()
                    .get(RedisConstants.SECKILL_STOCK_KEY + vid);
            if (redisStockStr == null) {
                continue; // 未预热的券跳过
            }
            int redisStock = Integer.parseInt(redisStockStr);
            int dbStock = sv.getStock();
            long dbOrders = query()
                    .eq("voucher_id", vid)
                    .in("status", 1, 2)          // 未支付 + 已支付 才占用库存
                    .count();
            Long redisOrders = stringRedisTemplate.opsForSet()
                    .size("seckill:order:" + vid);

            if (redisStock != dbStock || dbOrders != (redisOrders == null ? 0 : redisOrders)) {
                Map<String, Object> diff = new HashMap<>();
                diff.put("voucherId", vid);
                diff.put("redisStock", redisStock);
                diff.put("dbStock", dbStock);
                diff.put("dbOrderCount", dbOrders);
                diff.put("redisOrderCount", redisOrders);
                diffs.add(diff);
                // 记录对账差异表 + 告警日志
                StockReconcileLog logRow = new StockReconcileLog();
                logRow.setVoucherId(vid);
                logRow.setRedisStock(redisStock);
                logRow.setDbStock(dbStock);
                logRow.setDbOrderCount(dbOrders);
                logRow.setRedisOrderCount(redisOrders);
                logRow.setDiffDesc("redisStock=" + redisStock + ", dbStock=" + dbStock
                        + ", dbOrders=" + dbOrders + ", redisOrders=" + redisOrders);
                reconcileLogMapper.insert(logRow);
                log.warn("[对账] 发现库存差异：{}", logRow.getDiffDesc());
            }
        }
        Map<String, Object> report = new HashMap<>();
        report.put("checkedVouchers", vouchers.size());
        report.put("inconsistent", diffs.size());
        report.put("diffs", diffs);
        return report;
    }

    /**
     * 兜底关单：扫描创建超过 (15+5) 分钟仍为未支付的订单，逐单执行与关单消费者相同的 CAS 逻辑
     * 兜底场景：延迟消息丢失 / MQ 长时间不可用
     * 返回成功关单数
     */
    public int rescueTimeoutOrders() {
        LocalDateTime deadline = LocalDateTime.now().minusMinutes(
                RedisConstants.ORDER_UNPAID_TTL_MINUTES + 5);
        List<VoucherOrder> timeoutOrders = query()
                .eq("status", 1)
                .lt("create_time", deadline)
                .last("limit 200")
                .list();
        int rescued = 0;
        for (VoucherOrder order : timeoutOrders) {
            boolean closed = update()
                    .set("status", 4)
                    .eq("id", order.getId())
                    .eq("status", 1)
                    .update();
            if (closed) {
                seckillVoucherService.update()
                        .setSql("stock = stock + 1")
                        .eq("voucher_id", order.getVoucherId())
                        .update();
                stringRedisTemplate.opsForValue()
                        .increment(RedisConstants.SECKILL_STOCK_KEY + order.getVoucherId());
                stringRedisTemplate.opsForSet()
                        .remove("seckill:order:" + order.getVoucherId(),
                                order.getUserId().toString());
                rescued++;
            }
        }
        return rescued;
    }
```

新增注入与 import（`VoucherOrderServiceImpl`）：

```java
    @Resource
    private StockReconcileLogMapper reconcileLogMapper;

    // import
    import com.hmdp.entity.SeckillVoucher;
    import com.hmdp.entity.StockReconcileLog;
    import com.hmdp.mapper.StockReconcileLogMapper;
    import com.hmdp.utils.RedisConstants;
    import java.util.ArrayList;
    import java.util.HashMap;
```

**新增** `src/main/java/com/hmdp/entity/StockReconcileLog.java`（字段与 0.4 建表一一对应，`@TableId(type = IdType.AUTO)`，略——照抄 `OrderCompensation` 的样式即可）与 `StockReconcileLogMapper`。

**新增** `src/main/java/com/hmdp/task/OrderReconcileTask.java`：

```java
package com.hmdp.task;

import com.hmdp.service.impl.VoucherOrderServiceImpl;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Component;

/**
 * 每 5 分钟对账一次：库存一致性核对 + 超时未关单兜底
 */
@Slf4j
@Component
@RequiredArgsConstructor
public class OrderReconcileTask {

    private final VoucherOrderServiceImpl voucherOrderService;

    @Scheduled(cron = "0 */5 * * * ?")
    public void reconcile() {
        try {
            Object report = voucherOrderService.reconcile();
            int rescued = voucherOrderService.rescueTimeoutOrders();
            if (rescued > 0) {
                log.warn("[对账] 兜底关单 {} 笔（延迟消息丢失场景）", rescued);
            }
            log.debug("[对账] 完成：{}", report);
        } catch (Exception e) {
            log.error("[对账] 对账任务异常", e);
        }
    }
}
```

> 启动类 `HmDianPingApplication` 需追加 `@EnableScheduling`。单机定时任务即可；多实例部署时需换分布式调度（如 ShedLock）或仅允许一个实例开启 `@Scheduled`，这一点面试会被问到。

**手动触发接口** —— `VoucherOrderController` 新增：

```java
    /**
     * 手动触发对账（演示/压测后核对用）
     */
    @PostMapping("reconcile")
    public Result reconcile() {
        Map<String, Object> report = voucherOrderService.reconcile();
        report.put("rescuedTimeoutOrders", voucherOrderService.rescueTimeoutOrders());
        return Result.ok(report);
    }
```

## 1.10 秒杀券创建时预热 Redis 库存（配套增强）

| 项目 | 内容 |
|---|---|
| 接口说明 | 原课程新增秒杀券后需要手动向 Redis 灌库存。改造：`VoucherServiceImpl.addSeckillVoucher` 保存成功后自动写入 Redis 库存，消除"忘记预热则秒杀必失败"的隐患。 |
| 请求方式 | 复用已有 `POST /voucher/seckill`，无接口变化 |

**实现源码** —— 在 `VoucherServiceImpl.addSeckillVoucher` 保存 `SeckillVoucher` 成功后追加：

```java
        // 预热 Redis 库存（原课程需手动执行，此处自动化）
        stringRedisTemplate.opsForValue().set(
                RedisConstants.SECKILL_STOCK_KEY + voucher.getId(),
                String.valueOf(voucher.getStock()));
```

（`VoucherServiceImpl` 需注入 `StringRedisTemplate`。）

---

# 第 2 章 缓存模块

## 2.1 商铺详情查询（改造：Caffeine + Redis + DB 三级缓存）

| 项目 | 内容 |
|---|---|
| 接口说明 | 在原"逻辑过期防击穿"基础上增加 L1 本地缓存（Caffeine），热点商铺命中 L1 时省去一次 Redis 网络 RTT；L1 未命中走 L2 逻辑过期；L2 也未命中（未预热商铺）回源 DB 并走空值缓存防穿透。同时埋点浏览统计（UV + 热度榜）。 |
| 请求方式 | `GET`（接口签名不变，前端无感知） |
| 请求路径 | `/shop/{id}` |

**请求参数格式**：

```json
{ "id": "1" }
```

**返回值格式**：

```json
{
  "success": true,
  "data": {
    "id": "1", "name": "百味鲜", "typeId": "1", "area": "大关",
    "address": "金华路锦昌文华苑29号", "x": "120.149324", "y": "30.316632",
    "avgPrice": "80", "sold": "4215", "comments": "3035", "score": "45",
    "openHours": "10:00-22:00", "pics": "…", "createTime": "…", "updateTime": "…"
  }
}
```

**实现源码** —— 新增 `src/main/java/com/hmdp/config/LocalCacheConfig.java`（Caffeine 容器 + Pub/Sub 监听容器）：

```java
package com.hmdp.config;

import com.github.benmanes.caffeine.cache.Cache;
import com.github.benmanes.caffeine.cache.Caffeine;
import com.hmdp.entity.Shop;
import org.springframework.amqp.core.Binding;
import org.springframework.amqp.core.BindingBuilder;
import org.springframework.amqp.core.Queue;
import org.springframework.amqp.core.TopicExchange;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.redis.connection.RedisConnectionFactory;
import org.springframework.data.redis.core.StringRedisTemplate;
import org.springframework.data.redis.listener.ChannelTopic;
import org.springframework.data.redis.listener.RedisMessageListenerContainer;

import javax.annotation.Resource;
import java.util.concurrent.TimeUnit;

import static com.hmdp.utils.RedisConstants.CACHE_EVICT_TOPIC;

@Configuration
public class LocalCacheConfig {

    @Resource
    private ShopCacheEvictListener shopCacheEvictListener;

    /** L1 本地缓存：W-TinyLFU 淘汰，最大 1000 条，写后 60s 过期 */
    @Bean
    public Cache<String, Shop> shopLocalCache() {
        return Caffeine.newBuilder()
                .maximumSize(1000)
                .expireAfterWrite(60, TimeUnit.SECONDS)
                .build();
    }

    /** 缓存失效广播：交换机与队列（多节点部署时每个节点都会收到） */
    @Bean
    public TopicExchange cacheEvictExchange() {
        return new TopicExchange("cache.evict.exchange");
    }

    @Bean
    public Queue cacheEvictQueue() {
        // 匿名独占队列：每个应用实例各建一条，节点各自消费，互不影响
        return new Queue(CACHE_EVICT_TOPIC + ".queue." + System.currentTimeMillis(),
                false, true, true);
    }

    @Bean
    public Binding cacheEvictBinding(Queue cacheEvictQueue, TopicExchange cacheEvictExchange) {
        return BindingBuilder.bind(cacheEvictQueue).to(cacheEvictExchange).with("shop.evict");
    }

    /** Redis Pub/Sub 方案（二选一即可，默认使用 RabbitMQ 广播，见下方说明） */
    @Bean
    public RedisMessageListenerContainer redisMessageListenerContainer(
            RedisConnectionFactory connectionFactory) {
        RedisMessageListenerContainer container = new RedisMessageListenerContainer();
        container.setConnectionFactory(connectionFactory);
        container.addMessageListener(shopCacheEvictListener, new ChannelTopic(CACHE_EVICT_TOPIC));
        return container;
    }
}
```

**新增** `src/main/java/com/hmdp/config/ShopCacheEvictListener.java`：

```java
package com.hmdp.config;

import com.github.benmanes.caffeine.cache.Cache;
import com.hmdp.entity.Shop;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.data.redis.connection.Message;
import org.springframework.data.redis.connection.MessageListener;
import org.springframework.data.redis.core.StringRedisTemplate;
import org.springframework.stereotype.Component;

import static com.hmdp.utils.RedisConstants.CACHE_SHOP_KEY;

/**
 * 缓存失效广播订阅者：收到 shop.evict 消息后失效本节点的 L1/L2 缓存
 */
@Slf4j
@Component
@RequiredArgsConstructor
public class ShopCacheEvictListener implements MessageListener {

    private final Cache<String, Shop> shopLocalCache;
    private final StringRedisTemplate stringRedisTemplate;

    @Override
    public void onMessage(Message message, byte[] pattern) {
        String shopId = new String(message.getBody());
        // 失效 L1
        shopLocalCache.invalidate(CACHE_SHOP_KEY + shopId);
        // 失效 L2（逻辑过期缓存）
        stringRedisTemplate.delete(CACHE_SHOP_KEY + shopId);
        log.info("[缓存广播] 已失效商铺缓存，shopId={}", shopId);
    }
}
```

**改造** `ShopServiceImpl.queryById` 与 `update`：

```java
    @Resource
    private Cache<String, Shop> shopLocalCache;

    @Override
    public Result queryById(Long id) {
        String key = CACHE_SHOP_KEY + id;
        // ===== L1：Caffeine 本地缓存（W-TinyLFU，命中则零网络开销）=====
        Shop shop = shopLocalCache.getIfPresent(key);
        if (shop == null) {
            // ===== L2：Redis 逻辑过期缓存（防击穿：互斥重建 + 返回旧值）=====
            shop = clientClient.queryWithLogicalExpire(
                    CACHE_SHOP_KEY, id, Shop.class, this::getById,
                    CACHE_SHOP_TTL, TimeUnit.MINUTES);
            // ===== L2 miss（未预热商铺）→ 回源 DB（防穿透：空值缓存）=====
            if (shop == null) {
                shop = clientClient.queryWithPassThrough(
                        CACHE_SHOP_KEY, id, Shop.class, this::getById,
                        CACHE_SHOP_TTL, TimeUnit.MINUTES);
            }
            if (shop != null) {
                shopLocalCache.put(key, shop);
            }
        }
        if (shop == null) {
            return Result.fail("店铺不存在！");
        }
        // ===== 浏览埋点：UV（HLL 去重）+ 热度榜（ZSet 计数），供 2.5/2.6 与预热使用 =====
        recordShopView(id);
        return Result.ok(shop);
    }

    private void recordShopView(Long shopId) {
        String today = LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyy:MM:dd"));
        UserDTO user = UserHolder.getUser();
        String visitor = user != null ? String.valueOf(user.getId())
                : "guest:" + UUID.randomUUID().toString(true);
        // 日 UV：HyperLogLog 去重统计（误差 0.81%，万级 UV 仅 12KB）
        stringRedisTemplate.opsForHyperLogLog().add(RedisConstants.SHOP_UV_KEY + today, visitor);
        // 热度榜：浏览计数
        stringRedisTemplate.opsForZSet().incrementScore(RedisConstants.SHOP_HOT_KEY,
                shopId.toString(), 1);
    }

    @Override
    @Transactional
    public Result update(Shop shop) {
        Long id = shop.getId();
        if (id == null) {
            return Result.fail("店铺id不能为空");
        }
        // 1.更新数据库
        updateById(shop);
        // 2.删除 L2 Redis 缓存
        stringRedisTemplate.delete(CACHE_SHOP_KEY + id);
        // 3.失效本节点 L1 + 广播其他节点失效（多节点 L1 一致性）
        shopLocalCache.invalidate(CACHE_SHOP_KEY + id);
        stringRedisTemplate.convertAndSend(RedisConstants.CACHE_EVICT_TOPIC, String.valueOf(id));
        return Result.ok();
    }
```

新增 import（`ShopServiceImpl`）：

```java
import com.github.benmanes.caffeine.cache.Cache;
import com.hmdp.dto.UserDTO;
import com.hmdp.utils.UserHolder;
import cn.hutool.core.lang.UUID;
import java.time.format.DateTimeFormatter;
```

> 面试要点：① 为什么 L1 用短 TTL（60s）主动过期——**以秒级不一致换极致读性能**，主动失效 + 广播兜底一致性；② Caffeine 容量 1000 与 W-TinyLFU 淘汰；③ 集群下 L1 同步为何用广播——Pub/Sub / MQ 广播实现"删缓存"语义传递，避免各节点各自回源 DB 造成击穿。

## 2.2 热点商铺预热（ApplicationRunner）

| 项目 | 内容 |
|---|---|
| 接口说明 | 应用启动时将热点商铺（热度榜 TopN，无数据时兜底 DB 前 N 条）写入 L2 逻辑过期缓存 + L1 本地缓存，替代原 `ShopCacheTest` 的全量手动预热，同时修复"逻辑过期方案未预热商铺查不到"的缺陷。 |
| 请求方式 | 启动任务（无 HTTP 接口） |
| 请求路径 | -- |

**实现源码** —— 新增 `src/main/java/com/hmdp/task/HotShopWarmupRunner.java`：

```java
package com.hmdp.task;

import com.github.benmanes.caffeine.cache.Cache;
import com.hmdp.entity.Shop;
import com.hmdp.service.impl.ShopServiceImpl;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.boot.ApplicationArguments;
import org.springframework.boot.ApplicationRunner;
import org.springframework.data.redis.core.StringRedisTemplate;
import org.springframework.data.redis.core.ZSetOperations;
import org.springframework.stereotype.Component;

import java.util.List;
import java.util.Set;

import static com.hmdp.utils.RedisConstants.*;

/**
 * 热点商铺预热：启动时把 TopN 热点商铺装入 L1/L2 缓存
 */
@Slf4j
@Component
@RequiredArgsConstructor
public class HotShopWarmupRunner implements ApplicationRunner {

    private static final int WARM_UP_COUNT = 20;

    private final ShopServiceImpl shopService;
    private final Cache<String, Shop> shopLocalCache;
    private final StringRedisTemplate stringRedisTemplate;

    @Override
    public void run(ApplicationArguments args) {
        log.info("[预热] 开始预热热点商铺缓存...");
        // 1.取热度榜 TopN
        Set<String> hotIds = stringRedisTemplate.opsForZSet()
                .reverseRange(SHOP_HOT_KEY, 0, WARM_UP_COUNT - 1);
        List<Long> ids;
        if (hotIds == null || hotIds.isEmpty()) {
            // 2.冷启动兜底：DB 前 N 条
            ids = shopService.query().last("limit " + WARM_UP_COUNT).list()
                    .stream().map(Shop::getId).collect(java.util.stream.Collectors.toList());
        } else {
            ids = hotIds.stream().map(Long::valueOf)
                    .collect(java.util.stream.Collectors.toList());
        }
        // 3.逐个写入 L2（逻辑过期 30min）+ L1
        for (Long id : ids) {
            Shop shop = shopService.getById(id);
            if (shop == null) {
                continue;
            }
            shopService.saveShop2Redis(id, CACHE_SHOP_TTL * 60);   // 复用已有方法写 L2 逻辑过期
            shopLocalCache.put(CACHE_SHOP_KEY + id, shop);          // 写 L1
        }
        log.info("[预热] 热点商铺预热完成，共 {} 条", ids.size());
    }
}
```

## 2.3 热点商铺榜

| 项目 | 内容 |
|---|---|
| 接口说明 | 基于 ZSet 浏览计数返回浏览量 TopN 商铺。数据来源为 2.1 的浏览埋点，同时作为预热数据源。 |
| 请求方式 | `GET` |
| 请求路径 | `/shop/hot` |

**请求参数格式**：

```json
{ "top": "10" }   // 可选，默认 10
```

**返回值格式**：

```json
{
  "success": true,
  "data": [
    { "shopId": "1", "views": 1580, "shop": { "id": "1", "name": "百味鲜" } },
    { "shopId": "8", "views": 1204, "shop": { "id": "8", "name": "茶百道" } }
  ]
}
```

**实现源码** —— `ShopController` 新增：

```java
    /**
     * 热点商铺榜（浏览量 TopN）
     */
    @GetMapping("/hot")
    public Result queryHotShops(@RequestParam(value = "top", defaultValue = "10") Integer top) {
        return shopService.queryHotShops(top);
    }
```

`IShopService` 新增 `Result queryHotShops(Integer top);`，`ShopServiceImpl` 实现：

```java
    @Override
    public Result queryHotShops(Integer top) {
        Set<ZSetOperations.TypedTuple<String>> tuples = stringRedisTemplate.opsForZSet()
                .reverseRangeWithScores(RedisConstants.SHOP_HOT_KEY, 0, top - 1);
        if (tuples == null || tuples.isEmpty()) {
            return Result.ok(Collections.emptyList());
        }
        List<Map<String, Object>> result = new ArrayList<>(tuples.size());
        for (ZSetOperations.TypedTuple<String> tuple : tuples) {
            Long shopId = Long.valueOf(tuple.getValue());
            Shop shop = getById(shopId);
            if (shop == null) {
                continue;
            }
            Map<String, Object> item = new HashMap<>();
            item.put("shopId", shopId);
            item.put("views", tuple.getScore() == null ? 0 : tuple.getScore().intValue());
            item.put("shop", shop);
            result.add(item);
        }
        return Result.ok(result);
    }
```

## 2.4 商铺 UV 统计

| 项目 | 内容 |
|---|---|
| 接口说明 | 补齐课程 HyperLogLog UV 统计（当前代码缺失）：按天返回当日店铺浏览 UV（去重），同时给压测提供"日活"口径。 |
| 请求方式 | `GET` |
| 请求路径 | `/shop/uv` |

**请求参数格式**：

```json
{ "date": "2026:08:29" }   // 可选，默认今天
```

**返回值格式**：

```json
{ "success": true, "data": { "date": "2026:08:29", "uv": 1523 } }
```

**实现源码** —— `ShopController` 新增：

```java
    /**
     * 当日店铺浏览 UV（HyperLogLog 去重统计）
     */
    @GetMapping("/uv")
    public Result queryUv(@RequestParam(value = "date", required = false) String date) {
        String day = StrUtil.isNotBlank(date) ? date
                : LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyy:MM:dd"));
        Long uv = shopService.queryUv(day);
        Map<String, Object> data = new HashMap<>();
        data.put("date", day);
        data.put("uv", uv);
        return Result.ok(data);
    }
```

`ShopServiceImpl` 实现（`queryUv` 数据写入在 2.1 `recordShopView`）：

```java
    @Override
    public Long queryUv(String date) {
        return stringRedisTemplate.opsForHyperLogLog()
                .size(RedisConstants.SHOP_UV_KEY + date);
    }
```

---

# 第 3 章 限流组件

## 3.1 滑动窗口限流注解 + 切面（Redis + Lua，零新依赖）

| 项目 | 接口说明 |
|---|---|
| 接口说明 | 自定义 `@RateLimit` 注解 + AOP 切面 + Redis ZSet 滑动窗口 Lua 脚本，支持三种维度：`GLOBAL`（接口全局限流，保护系统容量）、`USER`（单用户频控，防脚本刷单）、`IP`。限流阈值必须来自压测塌陷区（见附录 B），拒绝拍脑袋。 |

**实现源码** —— 新增 `src/main/java/com/hmdp/annotation/RateLimit.java`：

```java
package com.hmdp.annotation;

import java.lang.annotation.*;

/**
 * 滑动窗口限流注解（Redis + Lua）
 */
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface RateLimit {

    /** 时间窗口（秒） */
    int windowSeconds() default 1;

    /** 窗口内最大请求数 */
    int maxCount() default 100;

    /** 限流维度 */
    LimitType limitType() default LimitType.GLOBAL;

    enum LimitType { GLOBAL, USER, IP }
}
```

**新增** `src/main/resources/ratelimit.lua`：

```lua
-- KEYS[1] = 限流 key
-- ARGV[1] = 当前时间戳(毫秒)  ARGV[2] = 窗口(毫秒)  ARGV[3] = 最大请求数
local key = KEYS[1]
local now = tonumber(ARGV[1])
local window = tonumber(ARGV[2])
local limit = tonumber(ARGV[3])

-- 1.移除窗口外的过期请求
redis.call('ZREMRANGEBYSCORE', key, 0, now - window)
-- 2.统计窗口内请求数
local count = redis.call('ZCARD', key)
if count < limit then
    -- 3.放行：记录本次请求（member 加随机数保证唯一）
    redis.call('ZADD', key, now, now .. '-' .. math.random(1000000))
    -- 4.窗口结束后自动清理整个 key
    redis.call('PEXPIRE', key, window)
    return 1
end
return 0
```

**新增** `src/main/java/com/hmdp/aspect/RateLimitAspect.java`：

```java
package com.hmdp.aspect;

import cn.hutool.core.util.StrUtil;
import com.hmdp.annotation.RateLimit;
import com.hmdp.dto.UserDTO;
import com.hmdp.exception.RateLimitException;
import com.hmdp.utils.RedisConstants;
import com.hmdp.utils.UserHolder;
import lombok.extern.slf4j.Slf4j;
import org.aspectj.lang.ProceedingJoinPoint;
import org.aspectj.lang.annotation.Around;
import org.aspectj.lang.annotation.Aspect;
import org.springframework.core.io.ClassPathResource;
import org.springframework.data.redis.core.StringRedisTemplate;
import org.springframework.data.redis.core.script.DefaultRedisScript;
import org.springframework.stereotype.Component;
import org.springframework.web.context.request.RequestContextHolder;
import org.springframework.web.context.request.ServletRequestAttributes;

import javax.annotation.PostConstruct;
import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import java.util.Collections;
import java.util.List;

/**
 * 滑动窗口限流切面
 */
@Slf4j
@Aspect
@Component
public class RateLimitAspect {

    private static final DefaultRedisScript<Long> RATE_LIMIT_SCRIPT;

    static {
        RATE_LIMIT_SCRIPT = new DefaultRedisScript<>();
        RATE_LIMIT_SCRIPT.setLocation(new ClassPathResource("ratelimit.lua"));
        RATE_LIMIT_SCRIPT.setResultType(Long.class);
    }

    @Resource
    private StringRedisTemplate stringRedisTemplate;

    @Around("@annotation(rateLimit)")
    public Object around(ProceedingJoinPoint pjp, RateLimit rateLimit) throws Throwable {
        String key = buildKey(pjp, rateLimit);
        long now = System.currentTimeMillis();
        Long allowed = stringRedisTemplate.execute(
                RATE_LIMIT_SCRIPT,
                Collections.singletonList(key),
                String.valueOf(now),
                String.valueOf(rateLimit.windowSeconds() * 1000L),
                String.valueOf(rateLimit.maxCount()));
        if (allowed == null || allowed == 0) {
            log.warn("[限流] 已拦截，key={}", key);
            throw new RateLimitException("请求过于火爆，请稍后再试");
        }
        return pjp.proceed();
    }

    private String buildKey(ProceedingJoinPoint pjp, RateLimit rateLimit) {
        String method = pjp.getSignature().getDeclaringType().getSimpleName()
                + "." + pjp.getSignature().getName();
        switch (rateLimit.limitType()) {
            case USER:
                UserDTO user = UserHolder.getUser();
                return RedisConstants.RATE_LIMIT_KEY + method + ":u:"
                        + (user == null ? "anonymous" : user.getId());
            case IP:
                HttpServletRequest request = ((ServletRequestAttributes) RequestContextHolder
                        .currentRequestAttributes()).getRequest();
                return RedisConstants.RATE_LIMIT_KEY + method + ":ip:" + request.getRemoteAddr();
            default:
                return RedisConstants.RATE_LIMIT_KEY + method;
        }
    }
}
```

**新增** `src/main/java/com/hmdp/exception/RateLimitException.java`：

```java
package com.hmdp.exception;

public class RateLimitException extends RuntimeException {
    public RateLimitException(String message) {
        super(message);
    }
}
```

## 3.2 限流响应格式（异常处理增强）

| 项目 | 内容 |
|---|---|
| 接口说明 | 限流触发时返回 HTTP 429 + 业务错误信息，前端展示友好提示。 |

**返回值格式**：

```json
{"success": false, "errorMsg": "请求过于火爆，请稍后再试"}
```

**实现源码** —— `WebExceptionAdvice` 追加：

```java
    @ExceptionHandler(RateLimitException.class)
    public Result handleRateLimitException(RateLimitException e, HttpServletResponse response) {
        response.setStatus(429);   // Too Many Requests
        log.warn("[限流] {}", e.getMessage());
        return Result.fail(e.getMessage());
    }
```

（新增 import：`com.hmdp.exception.RateLimitException`、`javax.servlet.http.HttpServletResponse`。）

## 3.3 秒杀接口接入示例

**接口说明**：全局维度保护系统容量（阈值 = 压测塌陷区 QPS × 0.8，压测后回填），用户维度防单用户脚本刷单。**双重限流叠加使用**。

**实现源码** —— `VoucherOrderController.seckillVoucher` 加注解：

```java
    // 全局：压测塌陷区 QPS×0.8（示例值 400，压测后替换）
    // 用户：每秒最多 5 次请求，防脚本刷单
    @RateLimit(windowSeconds = 1, maxCount = 400, limitType = RateLimit.LimitType.GLOBAL)
    @RateLimit(windowSeconds = 1, maxCount = 5, limitType = RateLimit.LimitType.USER)
    @PostMapping("seckill/{id}")
    public Result seckillVoucher(@PathVariable("id") Long voucherId) {
        return voucherOrderService.seckillVoucher(voucherId);
    }
```

> 面试要点：滑动窗口 vs 固定窗口（临界突刺问题）、令牌桶 vs 漏桶（突发流量容忍度）、为什么阈值来自压测——"限流值是容量规划结论，不是拍脑袋的数字"。

---

# 第 4 章 P2 进阶接口（选做）

## 4.1 P2-1 RocketMQ 事务消息下单（替换 RabbitMQ 下单链路）

| 项目 | 内容 |
|---|---|
| 接口说明 | 解决"Redis 预扣成功但消息发送失败"的原子性问题：采用 **RocketMQ 事务消息 + Redis 业务标记回查**。发送 half 消息 → 执行 Lua 预扣 → 本地事务确认提交/回滚；Broker 定时回查时以 Redis 判重集合（`SISMEMBER`）作为业务标记决定提交还是回滚（回滚时回补库存）。 |
| 请求方式 | RocketMQ 事务消息 |
| Topic 契约 | topic：`ORDER_CREATE_TOPIC`；producerGroup：`tx_order_producer_group`；consumerGroup：`order_create_group` |
| 请求路径 | -- |

**请求参数格式**（消息体，与 1.2 相同）：

```json
{
  "id": "1578558664933920770",
  "userId": "1010",
  "voucherId": "10"
}
```

**实现源码** —— `VoucherOrderServiceImpl` 新增事务消息发送（替换 1.1 的 `rabbitTemplate.convertAndSend` 段）：

```java
    @Resource
    private RocketMQTemplate rocketMQTemplate;

    /**
     * 事务消息发送：half 消息 → Lua 预扣（本地事务）→ 提交/回滚由 OrderTxListener 决定
     */
    public Result seckillVoucherTx(Long voucherId) {
        Long userId = UserHolder.getUser().getId();
        long orderId = redisIdWorker.nextId("order");
        Message<Long> msg = MessageBuilder
                .withPayload(orderId)
                .setHeader("userId", userId)
                .setHeader("voucherId", voucherId)
                .build();
        // arg 携带业务上下文，供 executeLocalTransaction 执行本地事务（Lua 预扣）
        SendResult sendResult = rocketMQTemplate.sendMessageInTransaction(
                "tx_order_producer_group", msg, new Object[]{orderId, userId, voucherId});
        if (SendStatus.SEND_OK.equals(sendResult.getSendStatus())) {
            return Result.ok(orderId);
        }
        return Result.fail("系统繁忙，请稍后再试");
    }
```

**新增** `src/main/java/com/hmdp/listener/OrderTxListener.java`：

```java
package com.hmdp.listener;

import cn.hutool.json.JSONUtil;
import com.hmdp.entity.VoucherOrder;
import com.hmdp.service.impl.VoucherOrderServiceImpl;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.apache.rocketmq.spring.annotation.RocketMQTransactionListener;
import org.apache.rocketmq.spring.core.RocketMQLocalTransactionListener;
import org.apache.rocketmq.spring.core.RocketMQTemplate;
import org.springframework.data.redis.core.StringRedisTemplate;
import org.springframework.messaging.Message;
import org.springframework.stereotype.Component;

import javax.annotation.Resource;

import static com.hmdp.utils.RedisConstants.SECKILL_STOCK_KEY;

/**
 * 事务消息监听器：
 *  - executeLocalTransaction：half 消息发送成功后执行本地事务（Lua 预扣），按结果提交/回滚
 *  - checkLocalTransaction：Broker 回查，以 Redis 判重集合作为业务标记决定提交/回滚
 */
@Slf4j
@Component
@RocketMQTransactionListener(txProducerGroup = "tx_order_producer_group")
@RequiredArgsConstructor
public class OrderTxListener implements RocketMQLocalTransactionListener {

    @Resource
    private VoucherOrderServiceImpl voucherOrderService;
    @Resource
    private StringRedisTemplate stringRedisTemplate;

    @Override
    public RocketMQLocalTransactionState executeLocalTransaction(Message msg, Object arg) {
        Object[] args = (Object[]) arg;
        Long orderId = (Long) args[0];
        Long userId = (Long) args[1];
        Long voucherId = (Long) args[2];
        try {
            // 本地事务：Lua 预扣（判库存+判重+预扣原子执行）
            Long result = voucherOrderService.executeSeckillLua(voucherId, userId, orderId);
            if (result == null || result != 0) {
                return RocketMQLocalTransactionState.ROLLBACK;   // 无资格：回滚 half 消息
            }
            return RocketMQLocalTransactionState.COMMIT;
        } catch (Exception e) {
            log.error("[事务消息] 本地事务异常，orderId={}", orderId, e);
            return RocketMQLocalTransactionState.UNKNOWN;        // 未知状态，等待回查
        }
    }

    @Override
    public RocketMQLocalTransactionState checkLocalTransaction(Message msg) {
        // 回查：Redis 判重集合里是否有该用户 = 预扣是否成功的业务标记
        Long voucherId = (Long) msg.getHeaders().get("voucherId");
        Long userId = (Long) msg.getHeaders().get("userId");
        Boolean member = stringRedisTemplate.opsForSet()
                .isMember("seckill:order:" + voucherId, String.valueOf(userId));
        boolean passed = Boolean.TRUE.equals(member);
        log.info("[事务消息] 回查，voucherId={}，userId={}，标记={}", voucherId, userId, passed);
        return passed ? RocketMQLocalTransactionState.COMMIT
                : RocketMQLocalTransactionState.ROLLBACK;
    }
}
```

消费端新增 Topic 监听（复用 1.2 落库逻辑）：

```java
    @RocketMQMessageListener(topic = "ORDER_CREATE_TOPIC", consumerGroup = "order_create_group")
    public class OrderCreateMqConsumer implements RocketMQListener<VoucherOrder> {
        @Override
        public void onMessage(VoucherOrder order) {
            voucherOrderService.createVoucherOrder(order);   // 与 1.2 相同的幂等落库
        }
    }
```

延迟关单同步升级：投递改为 `rocketMQTemplate.syncSend("ORDER_CLOSE_TOPIC", message, 3000, 15)`（延迟级别 15 = 20 分钟；RocketMQ 4.x 延迟级别固定为 1s/5s/10s/30s/1m/…/10m/20m/30m/1h/2h，无 15min 档位，取 20m 并把 `ORDER_UNPAID_TTL_MINUTES` 调整为 20，或升级 RocketMQ 5.x 使用任意时长定时消息）。

> 面试要点：half 消息"对消费者不可见"的实现（半消息topic换写 + 反查恢复）；为什么不能用"本地消息表"解决 Redis 场景（Redis 操作不在 DB 事务内，无 binlog 可依赖）；诚实讲清边界——**Redis 与 MySQL 无法强一致，事务消息只保证"消息与预扣标记的原子性"，最终仍靠对账兜底**。

## 4.2 P2-4 JWT + Redis 双 Token 登录

| 项目 | 内容 |
|---|---|
| 接口说明 | 登录改造为"短时效 JWT accessToken（2h，无状态）+ 长时效 refreshToken（7d，Redis 存储可吊销）"。保留主动踢人（黑名单）与无感续期能力。 |
| 请求方式 | `POST`（两个接口：login 改造返回双 token；refresh 刷新） |
| 请求路径 | `/user/login`（改造）、`/user/refresh`（新增） |

**请求参数格式**：

```json
// POST /user/login（不变）
{ "phone": "13698000114", "code": "123456" }

// POST /user/refresh
{ "refreshToken": "8f3a..." }
```

**返回值格式**：

```json
// login / refresh 统一返回
{
  "success": true,
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiJ9...",
    "refreshToken": "8f3a9c2e...",
    "expiresIn": 7200,
    "tokenType": "Bearer"
  }
}
```

**实现源码** —— 新增 `src/main/java/com/hmdp/utils/JwtUtil.java`：

```java
package com.hmdp.utils;

import cn.hutool.jwt.JWT;
import cn.hutool.jwt.JWTUtil;
import cn.hutool.jwt.signers.JWTSignerUtil;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

import java.util.HashMap;
import java.util.Map;

@Component
public class JwtUtil {

    @Value("${hmdp.jwt.secret}")
    private String secret;

    public static final long ACCESS_TTL_SECONDS = 2 * 3600L;

    /** 生成 accessToken：payload 携带 userId（无状态，服务端不存储） */
    public String createAccessToken(Long userId) {
        Map<String, Object> payload = new HashMap<>();
        payload.put("uid", userId);
        payload.put("exp", System.currentTimeMillis() / 1000 + ACCESS_TTL_SECONDS);
        return JWTUtil.createToken(payload, JWTSignerUtil.hs256(secret.getBytes()));
    }

    /** 校验并解析 accessToken，非法/过期返回 null */
    public Long verifyAccessToken(String token) {
        try {
            if (!JWTUtil.verify(token, JWTSignerUtil.hs256(secret.getBytes()))) {
                return null;
            }
            Object uid = JWT.of(token).getPayload("uid");
            return uid == null ? null : Long.valueOf(uid.toString());
        } catch (Exception e) {
            return null;
        }
    }
}
```

`application.yaml` 追加：

```yaml
hmdp:
  jwt:
    secret: ${JWT_SECRET:change-me-in-env-please}
```

`UserServiceImpl.login` 第 7 步（存储 Redis Hash 的部分）替换为双 Token：

```java
        // 7.生成双 Token
        String accessToken = jwtUtil.createAccessToken(user.getId());
        String refreshToken = UUID.randomUUID().toString(true);
        // refreshToken 存 Redis（可吊销/可踢人）：login:refresh:{userId}
        stringRedisTemplate.opsForValue().set(
                "login:refresh:" + user.getId(), refreshToken, 7, TimeUnit.DAYS);
        Map<String, Object> data = new HashMap<>();
        data.put("accessToken", accessToken);
        data.put("refreshToken", refreshToken);
        data.put("expiresIn", JwtUtil.ACCESS_TTL_SECONDS);
        data.put("tokenType", "Bearer");
        return Result.ok(data);
```

新增 `POST /user/refresh`（`UserController`）：

```java
    /**
     * 刷新 accessToken：refreshToken 有效（与 Redis 中一致）则签发新 token
     */
    @PostMapping("/refresh")
    public Result refresh(@RequestBody Map<String, String> body) {
        return userService.refresh(body.get("refreshToken"));
    }
```

`UserServiceImpl.refresh` 实现：

```java
    @Override
    public Result refresh(String refreshToken) {
        if (StrUtil.isBlank(refreshToken)) {
            return Result.fail("refreshToken 不能为空");
        }
        // 扫描匹配：demo 规模直接遍历用户 refresh key；生产应改为 refreshToken→userId 反查表
        Set<String> keys = stringRedisTemplate.keys("login:refresh:*");
        if (keys != null) {
            for (String key : keys) {
                if (refreshToken.equals(stringRedisTemplate.opsForValue().get(key))) {
                    Long userId = Long.valueOf(key.substring("login:refresh:".length()));
                    Map<String, Object> data = new HashMap<>();
                    data.put("accessToken", jwtUtil.createAccessToken(userId));
                    data.put("refreshToken", refreshToken);
                    data.put("expiresIn", JwtUtil.ACCESS_TTL_SECONDS);
                    data.put("tokenType", "Bearer");
                    return Result.ok(data);
                }
            }
        }
        return Result.fail("登录已过期，请重新登录");
    }
```

`LoginInterceptor.preHandle` 替换为 JWT 校验（Redis 黑名单兜底主动踢人）：

```java
public class LoginInterceptor implements HandlerInterceptor {

    @Resource
    private JwtUtil jwtUtil;
    @Resource
    private StringRedisTemplate stringRedisTemplate;

    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response,
                             Object handler) throws Exception {
        String token = request.getHeader("authorization");
        if (StrUtil.isNotBlank(token) && token.startsWith("Bearer ")) {
            token = token.substring(7);
        }
        Long userId = token == null ? null : jwtUtil.verifyAccessToken(token);
        if (userId != null && !Boolean.TRUE.equals(
                stringRedisTemplate.hasKey("login:jwt:blacklist:" + token))) {
            // 放行并装配用户上下文（查 Redis 用户 Hash，与原 RefreshTokenInterceptor 逻辑一致）
            Map<Object, Object> userMap = stringRedisTemplate.opsForHash()
                    .entries(RedisConstants.LOGIN_USER_KEY + userId);
            if (!userMap.isEmpty()) {
                UserDTO userDTO = BeanUtil.fillBeanWithMap(userMap, new UserDTO(), true);
                UserHolder.saveUser(userDTO);
                return true;
            }
        }
        response.setStatus(401);
        return false;
    }
}
```

> 注意：改为 JWT 后 `RefreshTokenInterceptor`（Redis Token 续期）职责由 accessToken 过期 + refresh 接口承担，`MvcConfig` 中可移除该拦截器；`logout` 接口同步补全：将当前 accessToken 写入 `login:jwt:blacklist:{token}`（TTL = accessToken 剩余有效期），并删除 `login:refresh:{userId}`。

## 4.3 P2-6 博客热度榜（`/blog/hot` 改造）

| 项目 | 内容 |
|---|---|
| 接口说明 | 原 `/blog/hot` 直接查 DB（liked 降序）。改造为 Redis ZSet 热度榜：定时任务每 10 分钟按 `score = liked×3 + comments×2 − 发帖小时数×0.5` 计算近 30 天博客热度写入 ZSet，接口从 ZSet 读取并回表组装。 |
| 请求方式 | `GET` |
| 请求路径 | `/blog/hot`（签名不变） |

**请求参数格式**：

```json
{ "current": "1" }
```

**返回值格式**：

```json
{
  "success": true,
  "data": [
    { "id": "5", "title": "探店｜这家火锅绝了", "liked": 120, "comments": 45, "shopId": "8" }
  ],
  "total": 100
}
```

**实现源码** —— 新增 `src/main/java/com/hmdp/task/BlogHeatRankTask.java`：

```java
package com.hmdp.task;

import com.hmdp.entity.Blog;
import com.hmdp.service.impl.BlogServiceImpl;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.data.redis.core.StringRedisTemplate;
import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Component;

import java.time.Duration;
import java.time.LocalDateTime;
import java.time.temporal.ChronoUnit;
import java.util.List;

import static com.hmdp.utils.SystemConstants.MAX_PAGE_SIZE;

/**
 * 博客热度榜：每 10 分钟刷新一次 ZSet（blog:hot）
 * score = liked*3 + comments*2 - 发帖小时数*0.5（时间衰减，下限 1）
 */
@Slf4j
@Component
@RequiredArgsConstructor
public class BlogHeatRankTask {

    public static final String BLOG_HOT_KEY = "blog:hot";

    private final BlogServiceImpl blogService;
    private final StringRedisTemplate stringRedisTemplate;

    @Scheduled(cron = "0 */10 * * * ?")
    public void refresh() {
        List<Blog> blogs = blogService.query()
                .ge("create_time", LocalDateTime.now().minusDays(30))
                .orderByDesc("liked")
                .last("limit " + MAX_PAGE_SIZE)
                .list();
        stringRedisTemplate.delete(BLOG_HOT_KEY);
        for (Blog blog : blogs) {
            long hours = Duration.between(blog.getCreateTime(), LocalDateTime.now())
                    .toHours();
            double score = Math.max(1,
                    blog.getLiked() * 3.0 + blog.getComments() * 2.0 - hours * 0.5);
            stringRedisTemplate.opsForZSet().add(BLOG_HOT_KEY, blog.getId().toString(), score);
        }
        log.info("[热度榜] 刷新完成，共 {} 条", blogs.size());
    }
}
```

`BlogController` 的 `/blog/hot` 改为从 ZSet 读取回表（保留 `current` 分页参数）：

```java
    @GetMapping("/hot")
    public Result queryHotBlog(@RequestParam(value = "current", defaultValue = "1") Integer current) {
        return blogService.queryHotBlog(current);
    }
```

`BlogServiceImpl.queryHotBlog` 实现（回表按 ZSet 顺序）：

```java
    @Override
    public Result queryHotBlog(Integer current) {
        int from = (current - 1) * SystemConstants.MAX_PAGE_SIZE;
        Set<String> ids = stringRedisTemplate.opsForZSet()
                .reverseRange(BlogHeatRankTask.BLOG_HOT_KEY, from, from + SystemConstants.MAX_PAGE_SIZE - 1);
        if (ids == null || ids.isEmpty()) {
            return Result.ok(Collections.emptyList());
        }
        String idStr = StrUtil.join(",", ids);
        List<Blog> blogs = query().in("id", ids)
                .last("order by field(id," + idStr + ")").list();
        blogs.forEach(this::fillBlogUser);   // 复用已有的用户信息填充方法
        return Result.ok(blogs);
    }
```

## 4.4 P2-7 动态秒杀 URL（防脚本直刷）

| 项目 | 内容 |
|---|---|
| 接口说明 | 秒杀开始后，前端先调本接口获取一次性动态 path（MD5，Redis 存 60s），再携带 path 请求秒杀接口；服务端校验通过才执行秒杀，使脚本无法直连秒杀地址。 |
| 请求方式 | `GET`（获取 path）+ `POST`（秒杀，路径变更） |
| 请求路径 | `/voucher-order/path/{voucherId}`；秒杀路径变更为 `/voucher-order/seckill/{voucherId}/{path}` |

**请求参数格式**：

```json
// GET /voucher-order/path/10 → 响应
{ "success": true, "data": "5d41402abc4b2a76b9719d911017c592" }

// POST /voucher-order/seckill/10/5d41402abc4b2a76b9719d911017c592
```

**返回值格式**：秒杀返回与 1.1 完全一致；path 非法时：

```json
{"success": false, "errorMsg": "非法请求"}
```

**实现源码** —— `VoucherOrderController` 调整：

```java
    /**
     * 获取动态秒杀路径（一次性，60s 有效）
     */
    @GetMapping("path/{id}")
    public Result getSeckillPath(@PathVariable("id") Long voucherId) {
        Long userId = UserHolder.getUser().getId();
        SeckillVoucher voucher = seckillVoucherService.getById(voucherId);
        if (voucher == null || voucher.getBeginTime().isAfter(LocalDateTime.now())
                || voucher.getEndTime().isBefore(LocalDateTime.now())) {
            return Result.fail("秒杀未开始或已结束");
        }
        String path = SecureUtil.md5(voucherId + "-" + userId + "-HMDP-SALT");
        stringRedisTemplate.opsForValue().set(
                RedisConstants.SECKILL_PATH_KEY + voucherId + ":" + userId,
                path, 60, TimeUnit.SECONDS);
        return Result.ok(path);
    }

    @RateLimit(windowSeconds = 1, maxCount = 400, limitType = RateLimit.LimitType.GLOBAL)
    @RateLimit(windowSeconds = 1, maxCount = 5, limitType = RateLimit.LimitType.USER)
    @PostMapping("seckill/{id}/{path}")
    public Result seckillVoucher(@PathVariable("id") Long voucherId,
                                 @PathVariable("path") String path) {
        Long userId = UserHolder.getUser().getId();
        String savedPath = stringRedisTemplate.opsForValue()
                .get(RedisConstants.SECKILL_PATH_KEY + voucherId + ":" + userId);
        // 一次性使用：立即删除，防重放
        stringRedisTemplate.delete(RedisConstants.SECKILL_PATH_KEY + voucherId + ":" + userId);
        if (savedPath == null || !savedPath.equals(path)) {
            return Result.fail("非法请求");
        }
        return voucherOrderService.seckillVoucher(voucherId);
    }
```

> 注意：此改动对原 seckill 路径做了破坏性变更（`seckill/{id}` → `seckill/{id}/{path}`），前端需同步增加一次 path 请求；旧路径可保留一个兼容入口（内部直接返回"请刷新页面"）。

## 4.5 P2-3 商铺搜索（Elasticsearch）

| 项目 | 内容 |
|---|---|
| 接口说明 | 将 `GET /shop/of/name` 的 `like '%name%'` 全表扫描升级为 ES 分词检索：IK 分词 + multi_match + 城市过滤 + 按销量排序，支持高亮。数据同步走"更新接口同步双写"（简版）或 Canal binlog（完整版）。 |
| 请求方式 | `GET` |
| 请求路径 | `/shop/search`（新增） |

**请求参数格式**：

```json
// Query 参数
{ "keyword": "美食", "typeId": "1", "current": "1" }
```

**返回值格式**：

```json
{
  "success": true,
  "data": [
    { "id": "1", "name": "百味鲜（<em>美食</em>城店）", "sold": "4215" }
  ],
  "total": 128
}
```

**索引 DSL**：

```json
PUT /shop
{
  "settings": { "analysis": { "analyzer": { "text_ik": { "tokenizer": "ik_max_word" } } } },
  "mappings": {
    "properties": {
      "id":      { "type": "long" },
      "name":    { "type": "text", "analyzer": "text_ik", "copy_to": "all" },
      "address": { "type": "keyword", "index": false },
      "typeId":  { "type": "long" },
      "sold":    { "type": "integer" },
      "score":   { "type": "integer" },
      "location":{ "type": "geo_point" },
      "all":     { "type": "text", "analyzer": "text_ik" }
    }
  }
}
```

**实现源码**（核心查询，配置类注入 `RestHighLevelClient` 略）：

```java
    @Override
    public Result search(String keyword, Integer typeId, Integer current) throws IOException {
        int from = (current - 1) * SystemConstants.DEFAULT_PAGE_SIZE;
        SearchRequest request = new SearchRequest("shop");
        BoolQueryBuilder bool = QueryBuilders.boolQuery();
        if (StrUtil.isBlank(keyword)) {
            bool.must(QueryBuilders.matchAllQuery());
        } else {
            bool.must(QueryBuilders.multiMatchQuery(keyword, "all"));
        }
        if (typeId != null) {
            bool.filter(QueryBuilders.termQuery("typeId", typeId));
        }
        request.source().query(bool)
                .from(from).size(SystemConstants.DEFAULT_PAGE_SIZE)
                .sort("sold", SortOrder.DESC)
                .highlighter(new HighlightBuilder().field("name").requireFieldFlag(false)
                        .preTags("<em>").postTags("</em>"));
        SearchResponse response = restHighLevelClient.search(request, RequestOptions.DEFAULT);
        List<Map<String, Object>> list = new ArrayList<>();
        for (SearchHit hit : response.getHits().getHits()) {
            Map<String, Object> source = hit.getSourceAsMap();
            // 用高亮结果覆盖 name
            HighlightField name = hit.getHighlightFields().get("name");
            if (name != null) {
                source.put("name", name.getFragments()[0].string());
            }
            list.add(source);
        }
        return Result.ok(list, response.getHits().getTotalHits().value);
    }
```

## 4.6 P2-5 可观测端点（Prometheus + Grafana）

| 项目 | 内容 |
|---|---|
| 接口说明 | 暴露 Prometheus 抓取端点，配合 Grafana 面板与告警规则，回答"项目部署在哪、怎么发现系统异常"。 |
| 请求方式 | `GET` |
| 请求路径 | `/actuator/prometheus`、`/actuator/health` |

**实现源码/配置**：

```yaml
# pom：spring-boot-starter-actuator + micrometer-registry-prometheus（见 0.1）
management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
  metrics:
    tags:
      application: ${spring.application.name}
```

```yaml
# prometheus.yml 抓取配置
scrape_configs:
  - job_name: "hmdp"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets: ["host.docker.internal:8081"]
```

```yaml
# Grafana 告警规则示例（秒杀接口 5xx 错误率 > 1% 持续 1 分钟触发）
- alert: HmdpSeckillHighErrorRate
  expr: sum(rate(http_server_requests_seconds_count{uri="/voucher-order/seckill/{id}",status=~"5.."}[1m]))
        / sum(rate(http_server_requests_seconds_count{uri="/voucher-order/seckill/{id}"}[1m])) > 0.01
  for: 1m
  labels: { severity: warning }
  annotations: { summary: "秒杀接口错误率超过 1%" }
```

---

# 附录 A 改造文件对照表

| # | 文件 | 动作 | 对应接口 |
|---|---|---|---|
| 1 | `pom.xml` | 修改 | 0.1 |
| 2 | `application.yaml` | 修改（rabbitmq 可靠性配置、jwt secret） | 0.2 / 4.2 |
| 3 | `utils/RedisConstants.java` | 修改（追加常量） | 0.3 |
| 4 | `db/upgrade_v1.sql` | 新增（唯一索引 + 2 张表） | 0.4 |
| 5 | `config/MqConstants.java` | 新增 | 0.5 |
| 6 | `config/QueueConfig.java` | 整体替换（4 队列拓扑） | 0.5 |
| 7 | `config/MqConfirmConfig.java` | 新增（发送确认） | 0.5 |
| 8 | `resources/seckill.lua` | 整体替换（删 XADD） | 0.6 |
| 9 | `service/impl/VoucherOrderServiceImpl.java` | 整体重构（1.1/1.2/1.4/1.9 源码合并） | 1.1 1.2 1.4 1.9 |
| 10 | `listener/OrderCreateListener.java` | 新增（含 x-death 重试计数） | 1.2 1.3 1.4 |
| 11 | `entity/OrderCompensation.java` + `mapper/OrderCompensationMapper.java` | 新增 | 1.4 |
| 12 | `listener/OrderCloseListener.java` | 新增（延迟关单 CAS + 双回补） | 1.5 |
| 13 | `listener/SeckillVoucherListener.java` | **删除**（原 QA/QD 消费者有重复落库 Bug） | 1.2 1.5 |
| 14 | `controller/VoucherOrderController.java` | 修改（pay/{id}/{status}/list/reconcile/path） | 1.6 1.7 1.8 1.9 4.4 |
| 15 | `entity/StockReconcileLog.java` + `mapper/StockReconcileLogMapper.java` | 新增 | 1.9 |
| 16 | `task/OrderReconcileTask.java` | 新增（`@EnableScheduling` 加在启动类） | 1.9 |
| 17 | `service/impl/VoucherServiceImpl.java` | 修改（创建秒杀券自动预热 Redis 库存） | 1.10 |
| 18 | `config/LocalCacheConfig.java` + `config/ShopCacheEvictListener.java` | 新增（Caffeine + 失效广播） | 2.1 |
| 19 | `service/impl/ShopServiceImpl.java` | 修改（三级缓存 + 埋点 + update 广播） | 2.1 2.3 2.4 |
| 20 | `task/HotShopWarmupRunner.java` | 新增（替代 ShopCacheTest 手动预热） | 2.2 |
| 21 | `annotation/RateLimit.java` + `aspect/RateLimitAspect.java` + `exception/RateLimitException.java` + `resources/ratelimit.lua` | 新增 | 3.1 |
| 22 | `config/WebExceptionAdvice.java` | 修改（429 处理） | 3.2 |
| 23 | `utils/JwtUtil.java` + `interceptor/LoginInterceptor.java` + `service/impl/UserServiceImpl.java` | 修改/新增（P2-4） | 4.2 |
| 24 | `task/BlogHeatRankTask.java` + `BlogController` + `BlogServiceImpl` | 新增/修改（P2-6） | 4.3 |
| 25 | ES 相关（P2-3）、RocketMQ 相关（P2-1）、actuator 配置（P2-5） | 选做 | 4.1 4.5 4.6 |

# 附录 B 验收与压测清单

每个接口完成后的验收口径（数字回填到简历，来源于落地清单 P1-3）：

| 接口 | 压测/验收方式 | 通过标准 |
|---|---|---|
| 1.1 秒杀下单 | JMeter 1000 线程 + `tokens.txt` CSV 参数化（已有 `VoucherOrderControllerTest` 造号） | 无异常、响应 time < 50ms（P99 需记录） |
| 1.2 异步落库 | 压测后核对 `count(tb_voucher_order) == (初始库存 - Redis 剩余库存)` | 完全相等、零重复订单（唯一索引无 DuplicateKey 之外的异常） |
| 1.3 重试 | 人为抛异常（如临时改 `createVoucherOrder` 首行 `int i = 1/0;`） | 消息 10s 后自动重试，最多 3 次 |
| 1.4 补偿 | 持续制造失败 | `tb_order_compensation` 有记录且 Redis 库存已回补 |
| 1.5 延迟关单 | 下单后不支付，等 15min（演示可临时把 TTL 调成 60s） | status 1→4、Redis/DB 库存回补、SREM 判重集合 |
| 1.6 支付 | 并发双击支付 / 先关单再支付 | 仅一笔成功；关单后支付返回"已超时关闭" |
| 1.9 对账 | 压测后手动 `POST /voucher-order/reconcile` | `inconsistent=0`；人为删 Redis 库存 key 能发现差异并落库 |
| 2.1 三级缓存 | 压测 `/shop/{id}`，对比改造前后 | P99 RT 明显下降；`update` 后各节点缓存失效（多实例验证广播） |
| 3.3 限流 | 超阈值压测 | 超限请求 429 快速失败，阈值内流量 RT 无明显劣化 |

压测环境记录模板（写进 README，面试必备）：机器配置 / MySQL·Redis·RabbitMQ 是否同机 / JMeter 是否同机 / JDK 版本 / 连接池参数（Lettuce max-active=30、Tomcat max-threads 默认 200）。

---

## 实施顺序建议

```
第 1 步  第 0 章（0.1~0.6 全局改造，半天）→ 第 1 章 1.1~1.2（核心链路，1 天）
第 2 步  1.3~1.5（重试/补偿/关单，1~2 天）→ 1.6~1.8（支付与查询，1 天）→ 1.9~1.10（对账，1 天）
第 3 步  第 2 章（多级缓存+预热+埋点，2 天）→ 第 3 章（限流，1 天）
第 4 步  压测留数（对照落地清单 P1-3）→ 按 4.x 选择 P2 项
```
