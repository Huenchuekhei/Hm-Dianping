# 黑马点评魔改升级落地清单（P1/P2）

> 生成日期：2026-08-29
> 适用代码库：`D:\JavaProject\hm-dianping`（SpringBoot 2.3.12 / Java 8 / Lettuce + Redisson 3.13.6 / MyBatis-Plus 3.4.3 / RabbitMQ）
> 配套文档：`README.md`、`黑马点评-项目包装与STAR简历.md`

---

## 0. 一页速览

| 编号 | 级别 | 魔改点 | 一句话目标 | 预估工作量 |
|---|---|---|---|---|
| P1-0 | P1 | 简历口径与代码对齐 + 技术债清理 | 消灭"一追问就穿帮"的硬伤 | 0.5 天 |
| P1-1 | P1 | 秒杀链路一致性加固：幂等 + 兜底 + 对账 | 把"用了 RabbitMQ"升级为"消息可靠性闭环" | 3~5 天 |
| P1-2 | P1 | 支付闭环：延迟关单 + CAS 支付 + 库存回补 | 补齐项目最大的业务断层（参考帖核心方向） | 3~5 天 |
| P1-3 | P1 | 全链路压测 + 量化指标（贯穿全程） | 用数据回答"部署在哪/QPS 多少/RT 多少" | 2~3 天 |
| P1-4 | P1 | Caffeine+Redis 多级缓存 + 热点预热 | 查询链路再降一个量级，衔接逻辑过期方案 | 2~3 天 |
| P1-5 | P1 | 限流：压测塌陷区 → Sentinel/Lua 滑动窗口 → 复测 | 形成"发现瓶颈→保护→验证"工程闭环 + 防刷 | 1~2 天 |
| P2-1 | P2 | 事务消息 / Outbox 本地消息表 | 解决"Redis 预扣 vs 发消息"的原子性 | 2~3 天 |
| P2-2 | P2 | Canal 监听 binlog 同步缓存 | 缓存失效从业务侵入升级为 binlog 订阅 | 2~3 天 |
| P2-3 | P2 | Elasticsearch 商铺搜索 | 把 `like '%%'` 升级为分词检索 + GEO 排序 | 2~3 天 |
| P2-4 | P2 | JWT+Redis 双 Token 登录改造 | 认证方案演进叙事（Session→JWT→混合） | 1~2 天 |
| P2-5 | P2 | Docker Compose + Prometheus/Grafana + SkyWalking | 可观测 + 可部署，正面回答"项目部署在哪" | 3~4 天 |
| P2-6 | P2 | Feed 流推拉结合 + 博客热度排行榜 | 社交模块从"纯推模式"升级为可讲取舍的方案 | 1~2 天 |
| P2-7 | P2 | 秒杀风控细节：动态秒杀 URL + 库存分段 | 防刷与热点 Key 的进阶处理 | 2~3 天 |

**建议节奏（约 3 周）**：第 1 周 P1-0 → P1-1 →（P1-3 基线压测）；第 2 周 P1-2 → P1-3 复测 → P1-4；第 3 周 P1-5 → 整理压测报告 → 从 P2 中选 1~2 项（推荐 P2-1、P2-5）。

---

## 1. 为什么黑马点评在简历上泛滥

### 1.1 泛滥的四个原因

1. **零门槛跟练**：B 站免费视频（72 讲）+ 资料齐全（SQL、前端静态页、Nginx 一键启动），跟着敲就能"完成项目"，不需要自己设计任何业务。
2. **知识点与面试大纲精确对齐**：缓存穿透/击穿/雪崩、分布式锁、Lua 原子性、消息队列、GEO/BitMap/HyperLogLog——每个模块都是一个 Redis 八股考点，被培训机构和"简历模板"文章大量推荐。
3. **模板话术病毒式传播**：CSDN/掘金上的《黑马点评+简历编写》类文章互相转载，导致简历描述一字不差："使用 Redis 的 ZSet 实现点赞排行榜、使用 Set 实现共同关注……"面试官一天能看到几十份一模一样的。
4. **成本收益失衡**：对求职者来说复制一份的成本远低于自研项目，于是形成"人人都有"的均衡。

### 1.2 面试官侧的真实证据（来自社区公开案例）

- NAOffer 案例：26 届南邮选手凭点评类项目冲大厂实习，被直接认定"烂大街项目——没有区别度，也没有加分项"（[naoffer.com](https://m.naoffer.com/article/detail/8491)）。
- 牛客网真实面经：美团后端日常实习一面，候选人写黑马点评，被围绕 Feed 流"疯狂拷打"后挂掉（[nowcoder.com](https://www.nowcoder.com/feed/main/detail/c79139d0e46a4f6694d17bfe59de629a)）。
- 牛客讨论帖：连续多轮面试被追问"项目部署在哪里？学校机房吗？用户有多少？延迟多少？"——答不上即暴露是跟练（[nowcoder.com](https://www.nowcoder.com/feed/main/detail/ed9f1491478748adba547f67d521bbad)）。
- JavaGuide《如何甄别应聘者的包装程度》：培训机构项目"千篇一律"，初筛即有成熟鉴别手段。

### 1.3 结论（本方案的策略基线）

社区的主流判断是一致的（博客园《三招教你爆改烂大街项目》、小红书"手把手教你改点评"系列作者）：

> **项目烂大街不是死刑，同质化 + 无深度才是。** 面试官看的是技术点、思考深度和量化结果，不是项目冷门与否。小红书系列作者原话："加上网上的大量魔改教程，技术深度绝对够了，关键在于你自己的思考。"

因此本方案两条腿走路：

1. **补深度闭环**——把已有魔改点（RabbitMQ 异步秒杀）从"能跑"加固到"经得起连环追问"（P1-1/P1-2）；
2. **造区分度**——用压测量化数据、支付业务闭环、限流工程闭环这些"大多数跟练者没有"的东西拉开差距（P1-3~P1-5）。

---

## 2. 本地项目现状快照（2026-08-29 扫描）

### 2.1 已完成（对标课程清单）

| 功能 | 状态 | 证据 |
|---|---|---|
| 短信登录 + 双层拦截器续期 | ✅ | `UserServiceImpl`、`RefreshTokenInterceptor` |
| 缓存穿透（空值缓存）/ 击穿（逻辑过期为主） | ✅ | `CacheClient.queryWithPassThrough/queryWithLogicalExpire` |
| 全局 ID 生成器 | ✅ | `RedisIdWorker` |
| 秒杀：乐观锁防超卖 + Lua 资格预检 | ✅ | `VoucherOrderServiceImpl.createVoucherOrder`、`seckill.lua` |
| 一人一单（DB 校验 + Lua Set 判重 + Redisson） | ✅ | `VoucherOrderServiceImpl:182,298` |
| **异步秒杀已改 RabbitMQ（超出课程）** | ✅ | `QueueConfig`（交换机 X + 死信 Y + TTL 队列 QA + 死信 QD）、`SeckillVoucherListener` |
| 点赞 ZSet 排行、Feed 流推模式 + 滚动分页 | ✅ | `BlogServiceImpl` |
| 共同关注（Set 交集）、GEO 附近商铺、BitMap 签到 | ✅ | `FollowServiceImpl`、`ShopServiceImpl:247`、`UserServiceImpl:123` |
| 并发测试基建 | 部分 | `VoucherOrderControllerTest`（MockMvc 批量造 1000 用户 token）；`tokens.txt` 目前为空，无 JMeter 脚本 |

### 2.2 硬伤与缺口（升级的直接动机）

| # | 问题 | 风险 |
|---|---|---|
| 1 | `SeckillVoucherListener` 落库时**绕过 `createVoucherOrder` 的 DB 层一人一单校验** | 重复下单风险；面试问"MQ 消费怎么幂等"即穿帮 |
| 2 | Redis 预扣库存与 DB 扣库存是**两套账，无对账** | 少卖/超卖无法发现；"消息丢了怎么办"答不上 |
| 3 | `seckill.lua` 仍 `XADD stream.orders`，但 Stream 消费者已注释 | 死代码；面试官翻代码会问"这行干嘛的" |
| 4 | `SimpleRedisLock` 引用 `unlock.lua`，实际文件名 `unLock.lua` | Windows 能跑、Linux 部署直接失败 |
| 5 | `application.yaml` 硬编码 MySQL root 密码 | 若开源仓库即泄露；也显不专业 |
| 6 | README 声称"redis 集群、tomcat 集群、MySQL 集群"、有 UV 统计（HyperLogLog），**代码里都没有** | 简历/文档口径与代码不符是最致命的穿帮点 |
| 7 | 无支付环节、无超时关单 | 秒杀没有商业闭环，业务故事断在"下单" |
| 8 | 无压测脚本、无量化指标、无限流 | "部署在哪/QPS 多少"三连问无解 |

---

## 3. 筛选标准（为什么是这些魔改点入选）

调研了掘金/CSDN/知乎/牛客/GitHub 上的公开魔改案例（hmdp-plus、garbagecodes/hmdp、haopengmai/dianping、Madgeaaaaa/hmdp 等）与面试复盘帖后，按四条标准筛选：

1. **有真实业务动机**——改造解决一个说得出口的问题，而不是"为了用组件"（据此否掉：为拆而拆的微服务化、数据量撑不起的分库分表）；
2. **面试追问链完整**——改完能自然引出 2~3 层深度追问且都有答案（如幂等→回补→对账→最终一致）；
3. **有可量化产出**——能落到压测数字、序列图、监控面板等"证据物"上；
4. **社区已泛滥的方向必须做得更深**——MQ 替换、多级缓存这类"第一眼魔改"别人都在写，我们的版本必须带可靠性/一致性细节才有意义。

> 反面清单见第 6 节。

---

## 4. P1：核心魔改点（优先完成，直接决定简历成色）

### P1-0 简历口径与代码对齐 + 技术债清理

- **改造目标**：让"简历/README 上的每一句话"都能在代码里找到对应物；修掉会在演示或部署时爆炸的低级问题。这不是加分项，是**防止一击致命的保险**。
- **实现思路**：
  1. README 删除或降级"集群"表述（改为"规划中/生产建议"）；补实现 UV 统计（`opsForHyperLogLog`，半天，属于课程补齐，不写进简历亮点）；
  2. 统一 `unLock.lua` 文件名与 `SimpleRedisLock` 中的引用（按 Linux 大小写敏感统一为小写）；
  3. 删除 `seckill.lua` 中的 `XADD` 死代码（或保留但把 Stream 消费代码一起清理干净）；
  4. `application.yaml` 密码改为环境变量注入 `${MYSQL_PWD}`（参考本仓库已有的 LLM Key 处理方式）；
  5. MySQL 驱动类名更新为 `com.mysql.cj.jdbc.Driver`。
- **简历加分点**：无直接加分，但"面试官翻仓库看到干净的代码"本身就是隐性加分；反之任何一处穿帮都可能让前面的努力归零。
- **验收**：`grep` 简历/README 中每个技术名词均能在代码中定位；`mvn test` 全绿。

### P1-1 秒杀链路一致性加固：幂等消费 + 失败兜底 + 定时对账

- **背景**：当前 RabbitMQ 版异步秒杀是社区"第一眼魔改"，但面试必问的三连——"重复消费怎么办？消费失败库存怎么回补？Redis 和 DB 库存不一致怎么发现？"——现在的代码都答不上（硬伤 1/2）。
- **改造目标**：把异步秒杀从"消息发出去就完事"升级为**幂等、重试、兜底、对账四件套齐全的可靠性闭环**。
- **实现思路**：
  1. **幂等**：`tb_voucher_order` 加 `(user_id, voucher_id)` 唯一索引；Listener 落库捕获 `DuplicateKeyException` 视为重复消息，直接 ack 丢弃。同时把课程版 `createVoucherOrder` 的一人一单校验挪进消费事务，Lua 预检只做"快速拦截"而非最终裁决；
  2. **重试与死信**：消费失败手动 nack；达到重试上限进死信队列 QD（`QueueConfig` 已建好），新增死信消费者：记录失败订单表 + 将 Redis 库存与 Lua 判重 Set 回补（SREM + INCR）；
  3. **对账**：定时任务（`@Scheduled` 起步）周期比对 `Redis 剩余库存 vs DB 剩余库存 vs 该券订单条数`，不一致打 WARN 日志并写对账差异表（这是"最终一致"叙事的兜底证据）；
  4. **消息确认**：生产端开启 RabbitMQ `publisher-confirm` + `return` 回调，失败落补偿表。
- **简历加分点**：
  - 简历句式从"使用 RabbitMQ 实现异步下单"（全网同款）变成"**针对异步秒杀的消息丢失/重复消费/Redis-DB 库存不一致问题，设计唯一索引幂等 + 死信回补 + 定时对账的最终一致性方案**"——这一句话就区分了跟练者和改造者；
  - 面试追问链完整：为什么会重复消费 → 幂等怎么做的 → 为什么不用分布式锁做幂等 → 回补时用户刚好又下单了怎么办 → 为什么不追求强一致（答：对账兜底 + 业务容忍）。
- **参考**：[掘金《黑马点评秒杀优化和场景补充》](https://juejin.cn/post/7556522678288203812)、[CSDN《RabbitMQ 优化秒杀》](https://blog.csdn.net/qq_35500719/article/details/149425736)、[hmdp-plus（Outbox+幂等设计）](https://github.com/java-up-up/hmdp-plus)。
- **验收**：JMeter 1000 并发压测：订单数 = 库存扣减数、零重复订单；人为 kill 消费者验证死信回补生效；画出可靠性时序图放进 README。

### P1-2 支付闭环：延迟关单 + CAS 支付 + 库存回补

- **背景**：参考帖（小红书"手把手教你改点评"系列第 3 篇《延迟关单和CAS支付》）指出的项目最大断层——秒杀完不能支付，且并发支付/长时间不付会出现超卖和脏数据。这也是 hmdp-plus 明确列入的"订单超时关闭"方向。
- **改造目标**：补全"下单 → 待支付 → 支付成功 / 超时关单 → 库存回补"的订单状态机，形成完整商业闭环。
- **实现思路**：
  1. `tb_voucher_order` 增加 `status`（1 待支付 / 2 已支付 / 3 已关闭）+ `pay_time`；
  2. **延迟关单**：首选复用现有 RabbitMQ TTL + 死信链路（`QueueConfig` 的 QA/QD 就是现成的延迟队列骨架，把 TTL 从 10s 调成 15min，订单号投 QA，死信消费者执行关单）；关单动作用 CAS：`UPDATE ... SET status=3 WHERE id=? AND status=1`，影响行数为 0 说明已支付，放弃关单；
  3. **CAS 支付**：支付回调同样 CAS 更新 `status 1→2`；若更新失败（已被关单）走补偿（退款/提示）；参考帖强调延迟消息本身可能丢失，**关单兜底要靠 P1-1 的对账任务补扫超时未支付订单**；
  4. **库存回补**：关单成功后 Redis `INCR` 库存 + `SREM` 判重 Set，允许用户重新抢；
  5. 支付接口本身可先做 Mock（本地沙箱），支付宝沙箱真实对接放到 P2（见 6.2 备注）。
- **简历加分点**：
  - 这是**业务深度**的最佳载体：状态机、并发控制（CAS）、延迟任务、资源回补四合一；
  - 延迟任务方案对比是面试高频考点，改完你能按梯度讲：定时轮询（扫全表、延迟高）→ JDK DelayQueue（单机、宕机丢）→ Redis 过期监听（不可靠）→ RabbitMQ 死信/TTL（当前方案）→ RocketMQ 延迟消息（P2-1 升级路径）→ Redisson DelayedQueue——**展示方案取舍能力比方案本身值钱**；
  - 直接对标参考帖系列与 hmdp-plus 的推荐改造，属于"面试官认可的魔改"。
- **参考**：小红书《手把手教你改点评 3-延迟关单和CAS支付》、[掘金《订单超时关闭的 11 种方式》](https://juejin.cn/post/7308894877461430322)、[hmdp-plus](https://github.com/java-up-up/hmdp-plus)。
- **验收**：演示"下单不支付 → 15 分钟后自动关单、Redis/DB 库存回补"；并发双击支付仅一笔成功；状态机图入 README。

### P1-3 全链路压测 + 量化指标（贯穿 P1-1/P1-4/P1-5 全程）

- **背景**：社区案例里跟练者最普遍的死法是答不出"部署在哪、QPS 多少、延迟多少"。**量化数据是区分"做过"和"看过"的唯一硬通货**。
- **改造目标**：为四个核心场景（商铺详情、秒杀下单、Feed 流、点赞排行）建立可复现的压测基线，并在每次改造后产出前后对比。
- **实现思路**：
  1. JMeter 线程组 1000 线程/1s 内启动；用现有 `VoucherOrderControllerTest` 批量生成的 `tokens.txt` 转 CSV 做参数化登录态（先把测试跑起来填上 token）；
  2. 记录**基线**（同步版秒杀，课程原始逻辑）→ 每完成一项 P1 改造**复测**，得到对照表：QPS、平均 RT、P99 RT、错误率；
  3. 每个数字注明环境：本机配置（CPU/内存/磁盘）、MySQL/Redis 是否同机、JMeter 是否同机（避免被问"你这数字怎么来的"时露馅）；
  4. 选一个真实瓶颈做一次深度排查（推荐 Tomcat 线程池或 Redis Lettuce 连接池参数，或某条慢 SQL 加索引），用 Arthas `trace/dashboard` 或 `EXPLAIN` 留下证据截图；
  5. 产出物：`benchmark/` 目录放 `.jmx` 脚本、结果 CSV、对照表 Markdown。
- **简历加分点**：
  - 简历每条亮点都能带数字："秒杀接口经 1000 并发压测，QPS 由 X 提升至 Y，P99 RT 由 Xms 降至 Yms"；
  - 这是面试官验证真实性的第一抓手，也是所有话术的"锚点"。
- **参考**：[掘金《黑马点评异步秒杀优化》（JMeter 实操，含 CSV 用户导入）](https://juejin.cn/post/7498970712474189865)、[CSDN《如何针对项目技术难点准备面试》](https://blog.csdn.net/qq_32792547/article/details/142886458)。
- **验收**：一张前后对比表 + 能解释每个数字的成因（改动点→指标变化）。

### P1-4 Caffeine + Redis 多级缓存 + 热点商铺预热

- **背景**：商铺详情查询已实现逻辑过期防击穿，但每次仍要打一次 Redis（一次网络 RTT）。多级缓存是社区公认性价比最高的缓存向魔改，且与现有方案衔接自然。
- **改造目标**：热点商铺查询变为"L1 Caffeine（JVM 内）→ L2 Redis → DB"三级回源，消除 Redis 网络 RTT，并解决集群下本地缓存一致性问题。
- **实现思路**：
  1. 引入 Caffeine，`ShopServiceImpl.queryById` 前置一层本地缓存（maximumSize≈1000、expireAfterWrite≈60s——短 TTL 主动容忍短暂不一致，换取极致读性能，这句取舍要在面试里主动讲）；
  2. 一致性：Redis Pub/Sub 广播"evict:shop:{id}"事件，所有实例收到后删本地缓存（多实例同步的标准低成本做法）；
  3. 预热：`ApplicationRunner` 启动时把 TopN 热点商铺装入 L1/L2——热点来源可以接 `shop` 浏览统计（顺手把 HyperLogLog/UV 数据用起来），替换现在 `ShopCacheTest` 里的全量预热；
  4. 逻辑过期与互斥锁只在 L2 未命中回源时生效，L1 命中直接返回。
- **简历加分点**：
  - 从课程"缓存三件套"自然进阶到工业界方案，能接住"本地缓存集群下怎么同步""容量和淘汰策略怎么定（W-TinyLFU）""为什么不直接加大 Redis"三类高频追问；
  - 预热的 TopN 统计把"UV 统计"这个课程点从孤立的八股变成有用途的组件——**组件间形成体系是高阶印象分**。
- **参考**：[CSDN《Caffeine+Redis 二级缓存》](https://blog.csdn.net/m0_52031708/article/details/142862864)、[CSDN《Canal 实现多级缓存同步》](https://blog.csdn.net/m0_52031708/article/details/145672266)（P2-2 的升级路径）、[javaup.chat hmdp-plus 缓存架构](https://javaup.chat/hmdp-plus/cache-architecture/multi-level-cache/)。
- **验收**：JMeter 对比商铺详情接口：命中 L1 后 RT 从 ~2ms 降至亚毫秒； kill 一个实例验证 Pub/Sub 同步逻辑。

### P1-5 限流：压测塌陷区 → Sentinel / Lua 滑动窗口 → 复压验证

- **背景**：有了 P1-3 的压测数据，限流阈值才"有出处"。社区公认最高分的叙事是"压测→找到性能塌陷点→加保护→复测验证"的闭环；同时按 userId 限流天然引出防刷风控话题。
- **改造目标**：秒杀接口获得两级保护——全局容量限流（保系统）+ 单用户频控（防刷单），并且阈值全部来自实测。
- **实现思路**：
  1. 从 P1-3 数据找到秒杀接口塌陷区（RT 开始陡增的 QPS 阈值）；
  2. 方案 A（推荐，零新依赖）：自定义 `@RateLimit` 注解 + AOP + Redis Lua 滑动窗口，支持两个维度：接口全局 QPS（塌陷区 ×0.8）与 `userId` 维度（如 5 次/秒，防脚本刷单）；
  3. 方案 B：引入 `spring-cloud-starter-alibaba-sentinel`（单机也能跑），配 QPS 流控 + 熔断降级 + fallback 返回"活动太火爆"；两者都做亦可（A 防刷、B 兜底）；
  4. 复压验证：超阈值流量被快速失败（HTTP 429/友好提示），阈值内流量不受影响，指标记录进对照表。
- **简历加分点**：
  - "限流阈值来自压测塌陷区而非拍脑袋"本身就是区分度；能继续讲令牌桶 vs 漏桶 vs 滑动窗口的取舍、Sentinel 与 Nginx 限流的层级差异；
  - userId 频控把话题引向风控（登录频率、黑名单、设备指纹——美团风控文章是现成谈资）。
- **参考**：[CSDN《压测秒杀接口算出性能塌陷区，用 Sentinel 限流》](https://blog.csdn.net/m0_52031708/article/details/145759792)、[知乎《如何设计一个秒杀系统》](https://zhuanlan.zhihu.com/p/1976245478703247986)。
- **验收**：规则表 + 复压数据入对照表；演示超限请求被拦截且正常请求无感。

---

## 5. P2：进阶魔改点（P1 完成后按需选做，每项都能独立成亮点）

### P2-1 事务消息 / Outbox：解决"Redis 预扣 vs 发消息"的原子性

- **改造目标**：Lua 预扣库存后进程宕机会少卖（消息丢了）、发送超时假失败会多卖——用事务消息或本地消息表把"预扣"与"发消息"绑成原子操作。这是参考帖第 2 篇《事务消息》的核心命题，也是 P1-1 的终极形态。
- **实现思路**（二选一）：
  - **RocketMQ 事务消息**（参考帖方案，简历辨识度更高）：half 消息 → 执行 Redis 预扣 → commit/rollback；Broker 回查时用 Redis 里的业务标记（如 `sismember` 判重 Set）决定提交还是回滚；
  - **RabbitMQ + 本地消息表**（改动最小）：预扣成功后同事务写一条 outbox 记录，定时任务扫描重发，消费端靠 P1-1 幂等兜底。
- **简历加分点**：能讲 half 消息/回查机制、两种方案对比（最终一致 vs 准实时）、并诚实说出边界："Redis 与 MySQL 无法强一致，定时对账兜底"——这句"承认局限"是高阶加分项。
- **参考**：小红书《手把手教你改点评 2——事务消息》、[hmdp-plus](https://github.com/java-up-up/hmdp-plus)。

### P2-2 Canal 监听 binlog 同步缓存（零业务侵入）

- **改造目标**：商铺缓存失效从"Service 手写删缓存/延迟双删"升级为 Canal 订阅 binlog → MQ → 统一失效缓存，业务代码零侵入。
- **实现思路**：MySQL 开 ROW binlog → Canal Server 伪装 slave 拉取 `tb_shop` 变更 → 投递到现有 RabbitMQ → 新监听器删 Redis key + 广播删 Caffeine（衔接 P1-4 的 Pub/Sub）。
- **简历加分点**：缓存一致性的"标准进阶答案"；必准备回应："数据量这么小为什么用 Canal"（口径：模拟运营侧批量改价/改店的场景，解耦写入路径）。延迟双删的局限（延迟时间拍脑袋、二次删除仍可能失败）要能先说清——它是被 Canal 替换的"前朝方案"。
- **参考**：[CSDN《Canal 实现多级缓存同步》](https://blog.csdn.net/m0_52031708/article/details/145672266)、[博客园 binlog+Canal+Redis 教程](https://www.cnblogs.com/JavaYuYin/p/18006205)。

### P2-3 Elasticsearch 商铺搜索

- **改造目标**：把 `queryShopByName` 的 `like '%name%'` 全表扫描升级为 ES 分词检索，并叠加 GEO 距离排序。
- **实现思路**：商铺数据经 MQ 双写/LD 同步进 ES；IK 分词 + `multi_match` + 城市星级 `filter` + `geo_distance` 排序（与课程 GEO 呼应）；保留 MySQL 为主存储。
- **简历加分点**：搜索是引入 ES"正当性最强"的场景；准备好深分页（`search_after`）、索引设计、同步延迟三类追问。
- **风险提示**：属"锦上添花"，P1 没完成前不做。

### P2-4 JWT + Redis 双 Token 登录改造

- **改造目标**：认证从"纯 Redis 会话"演进为"JWT（无状态）+ Redis（可吊销）"混合方案，保留主动踢人和续期能力。
- **实现思路**：access_token（JWT，2h）+ refresh_token（Redis，7d）；现有双层拦截器结构保留，第一层做 JWT 校验 + Redis 黑名单检查。
- **简历加分点**：认证演进叙事完整（Session→JWT→混合）；注意此改造网上也很多，区分度有限，适合作为 P1 间隙的填充项。小心双 Token 的坑（旧 token 安全黑洞、并发刷新）要能讲。
- **参考**：[CSDN《JWT+Redis 自动续期》](https://blog.csdn.net/m0_52031708/article/details/145642681)。

### P2-5 可观测 + 部署工程化：Docker Compose + Prometheus/Grafana + SkyWalking

- **改造目标**：一次回答三连问的另一半——"部署在哪？你怎么知道系统出问题了？"
- **实现思路**：`docker-compose.yml` 编排 app/MySQL/Redis/RabbitMQ/Nginx；Micrometer 暴露 `/actuator/prometheus`，Grafana 导入 JVM/应用面板并配一条真实告警规则（如错误率 >1%）；应用挂 skywalking-javaagent，给出秒杀全链路拓扑（网关→应用→Redis→MQ→MySQL）截图。
- **简历加分点**：与 P1-3 的瓶颈排查故事对上（"我是从 SkyWalking 拓扑上发现 XX 慢调用的"）；Docker Compose 让"项目可复现部署"成为仓库里看得见的事实。
- **风险提示**：必须搭配至少一个真实排查/告警案例，否则只是"装了个 Grafana"。
- **参考**：[博客园 Prometheus+Grafana+SkyWalking 部署](https://www.cnblogs.com/user-sunli/p/14926945.html)。

### P2-6 Feed 流推拉结合 + 博客热度排行榜

- **改造目标**：Feed 从课程纯推模式升级为推拉结合；新增基于 ZSet 的热度榜。
- **实现思路**：粉丝数 >N 的作者改拉模式（读时聚合自己收件箱 + 作者 Timeline），普通用户保留推模式；热度分 = 点赞 + 评论×权重 − 时间衰减，ZSet 存储、定时任务刷新。
- **简历加分点**：正面接住美团面经里"Feed 流被疯狂拷打"的考点："明星发帖怎么办？"→ 大 V 拉模式/读时聚合；"收件箱爆炸怎么办？"→ 收件箱截断 + 分页游标。热度榜展示业务抽象与调度设计。
- **参考**：[牛客美团一面凉经（Feed 流考点）](https://www.nowcoder.com/feed/main/detail/c79139d0e46a4f6694d17bfe59de629a)。

### P2-7 秒杀风控细节：动态秒杀 URL + 库存分段

- **改造目标**：防脚本与热点 Key 进阶处理。
- **实现思路**：秒杀开始前通过校验接口下发动态路径（Redis 存 MD5 标识），下单先校验路径合法性；库存热点可选拆分为 N 个子 key（Hash 分段）降低单 Key 竞争，为 Redis Cluster 场景预热。
- **简历加分点**：对接 hmdp-plus 的"动态令牌"方向；热点 Key 问题（本地缓存/分段/随机后缀）是 Redis 进阶面试常客。

---

## 6. 明确不推荐（防"包装感"反噬清单）

| 不推荐项 | 原因 |
|---|---|
| ShardingSphere 分库分表 | 订单量撑不起动机，面试官一句"多少数据了要分表"就穿帮；社区公认"为简历而做"的重灾区 |
| 为拆而拆的微服务（Spring Cloud 五服务拆分） | 代码量小、无治理收益；除非配套 Nacos/Sentinel/链路追踪/分布式事务完整讲出"单体解决不了什么"，否则负分 |
| K8s 部署 | 单机项目 Compose 足够；被问"为什么不用 Compose"难以自圆其说 |
| Seata | 单体单库没有分布式事务痛点；它是 P2-1 之后"拆分了才需要"的话题 |
| 无问题的组件名词堆砌 | "使用了 Redis/Kafka/ES/Sentinel/Seata"式罗列是 JavaGuide 总结的典型包装特征，初筛即被识别 |
| 编造压测数字 | 数字必须可复现：被追问压测机配置、线程模型、数据库参数时答不上即全盘崩塌 |

---

## 7. 迭代节奏建议（约 3 周可完成 P1 全部）

```
第 1 周  P1-0 口径对齐（0.5d）→ P1-3 基线压测（先测同步版）→ P1-1 一致性加固（3~5d）
第 2 周  P1-2 支付闭环（3~5d）→ P1-3 复测对比 → P1-4 多级缓存（2~3d）
第 3 周  P1-5 限流闭环（1~2d）→ 整理压测对照表/时序图/README → 启动 P2-1（事务消息）
之后     按面试时间倒推，从 P2-2/2-3/2-5/2-6 中再选 1~2 项
```

原则：**每一项改造完成立刻压测留数、更新 README 与简历文案**，不要攒到最后补数据（数字会忘、环境会变）。

---

## 8. 简历文案（P1 完成后可直接套用）

项目一句话定位（建议换壳重定位，保留技术内核）：
> **"XX 生活/校园服务平台"**（如：校园生活服务与优惠券营销平台）：基于 SpringBoot + Redis + RabbitMQ 的本地生活平台，核心场景为高并发优惠券秒杀与热点商铺查询。

亮点条目模板（数字用 P1-3 实测值替换，禁止编造）：

1. **秒杀可靠性**：设计"Lua 脚本资格预检 + RabbitMQ 异步下单"秒杀链路，针对消息重复/丢失与 Redis-DB 库存不一致问题，实现唯一索引幂等消费、死信回补库存、定时对账三重保障；1000 并发压测下零重复订单，QPS 由【X】提升至【Y】。
2. **订单闭环**：补齐支付状态机（待支付/已支付/已关闭），基于 RabbitMQ TTL+死信实现 15 分钟延迟关单，关单与支付均以数据库 CAS 保证并发安全并回补库存；超时未支付订单由对账任务兜底清理，资源零泄漏。
3. **多级缓存**：商铺查询构建 Caffeine（L1）+ Redis（L2）+ MySQL 三级缓存，Pub/Sub 广播失效保证集群一致性，启动时按热度 TopN 预热；热点接口 P99 RT 由【X】ms 降至【Y】ms。
4. **限流与压测**：通过 JMeter 压测定位秒杀接口性能塌陷区（QPS≈【X】），据此配置滑动窗口限流（全局容量 + 单用户频控双维度），复压验证超限流量快速失败、阈值内流量无损。

> 配套面试叙事主线：`setnx 手写锁 → 暴露不可重入/误删/续期问题 → Redisson → Lua 预检后锁的角色变化 → MQ 削峰 → 可靠性闭环 → 对账兜底`，全程用演进链引导面试官往准备好的方向追问。

---

## 9. 参考来源

**参考帖（用户提供，小红书"手把手教你改点评"系列）**
- [改点评 2——事务消息（RocketMQ 事务消息 + Redis 业务标记回查）](https://www.xiaohongshu.com/explore/6a911c52000000002a03868d)
- [改点评 3——延迟关单和 CAS 支付（延迟消息 + CAS 乐观锁 + 对账兜底）](https://www.xiaohongshu.com/explore/6a87ea020000000008016460)

**系统性魔改仓库**
- [java-up-up/hmdp-plus](https://github.com/java-up-up/hmdp-plus)（+ [javaup.chat 文档站](https://javaup.chat/hmdp-plus/overview/project-change/)）：超时关单、Outbox+幂等、动态令牌、候补排队
- [garbagecodes/hmdp](https://github.com/garbagecodes/hmdp)、[haopengmai/dianping](https://github.com/haopengmai/dianping)、[Madgeaaaaa/hmdp](https://github.com/Madgeaaaaa/hmdp)

**同质化证据与面试官视角**
- [NAOffer：26 届南邮选手写点评项目被劝阻](https://m.naoffer.com/article/detail/8491)
- [牛客：美团一面凉经（Feed 流拷打）](https://www.nowcoder.com/feed/main/detail/c79139d0e46a4f6694d17bfe59de629a)、[牛客：部署在哪三连问](https://www.nowcoder.com/feed/main/detail/ed9f1491478748adba547f67d521bbad)、[牛客：锁与 MQ 先讲哪个的争议](https://www.nowcoder.com/feed/main/detail/97191e95ea3844959f1e6494e7fe69b8)
- [博客园《三招教你爆改烂大街项目》](https://www.cnblogs.com/huijiayangzhu/p/18593374)、[JavaGuide《如何甄别应聘者的包装程度》](https://javaguide.cn/high-quality-technical-articles/interview/screen-candidates-for-packaging.html)

**专项实现参考**
- MQ/秒杀：[掘金·秒杀优化与场景补充](https://juejin.cn/post/7556522678288203812)、[CSDN·RabbitMQ 优化秒杀](https://blog.csdn.net/qq_35500719/article/details/149425736)、[掘金·JMeter 异步秒杀压测实操](https://juejin.cn/post/7498970712474189865)
- 缓存：[CSDN·Caffeine+Redis 二级缓存](https://blog.csdn.net/m0_52031708/article/details/142862864)、[CSDN·Canal 多级缓存同步](https://blog.csdn.net/m0_52031708/article/details/145672266)
- 限流：[CSDN·压测塌陷区 + Sentinel](https://blog.csdn.net/m0_52031708/article/details/145759792)、[知乎·秒杀系统设计](https://zhuanlan.zhihu.com/p/1976245478703247986)
- 订单/支付：[掘金·订单超时关闭 11 种方式](https://juejin.cn/post/7308894877461430322)、[掘金·支付宝沙箱支付教程](https://juejin.cn/post/7494181442106884130)
- 锁：[Seven·Redis 分布式锁演进](https://seven97.top/database/redis/05-implementdistributedlocks.html)、[CSDN·Redisson 15 问](https://blog.csdn.net/weixin_45630885/article/details/125088885)
- 可观测：[博客园·Prometheus+Grafana+SkyWalking](https://www.cnblogs.com/user-sunli/p/14926945.html)
